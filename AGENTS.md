# RuleReader 编码 Agent 工作约定

本文件只规定 AI 编码 Agent 在本仓库中的长期行为、工程边界和当前阶段门禁。产品需求、验收标准、技术方案、数据结构和实施细节由 `docs/` 中的项目文档维护；不要把这些内容重复复制到本文件。

## 1. 基本行为

- 始终使用中文沟通；代码、标识符、API、数据库字段、配置键和提交信息使用英文。
- 先阅读项目文档、现有代码和测试，再决定实现方式。文档已有答案时，不重复询问用户。
- 只修改当前任务所需内容，不顺手重构，不提前实现未来阶段，不为尚未发生的需求搭建复杂框架。
- 对缺失信息先在仓库中检索；只有无法从文档、代码或测试中确定且不同选择会显著改变结果时，才向用户提问。
- 无法在当前任务内完成的内容必须明确记录为遗留项，不承诺后台继续处理。

## 2. 权威信息与冲突处理

开始任何任务前，按以下顺序定位上下文：

1. `docs/进度文档.md`：确认当前阶段、当前任务、前置依赖、阻塞和下一任务。
2. 与任务直接关联的 `REQ`、`BIZ`、`DEV` 或 `BUG` 文档：确认本次范围、决策和验收标准。
3. `docs/PRD.md`：确认产品目标、功能边界和非目标。
4. `docs/技术设计文档.md`：确认架构、模块职责、接口和约束。
5. `docs/实施文档.md`：确认阶段顺序和各阶段 DoD。
6. `project_release_double_agent_solution.md`：了解长期目标架构；它不是当前阶段的实施清单。
7. 现有代码、迁移、配置和测试：确认仓库已经采用的实现模式。

冲突处理规则：

- 用户本次明确要求优先于仓库内既有项目文档；需要同步修改受影响文档，不能只在对话中保留决定。
- 对同一产品问题，任务专属且已确认的 `REQ`/`BIZ` 优先于通用 PRD，PRD 优先于技术设计，技术设计优先于实施与进度记录。
- `AGENTS.md` 约束 Agent 行为和代码边界，不替代产品需求。
- 代码与高优先级文档不一致时，不得静默猜测或迁就旧代码；先说明差异，再按高优先级目标同步文档、代码和测试。

## 3. 当前阶段与技术栈硬边界

项目技术栈冻结为：

技术栈决策以 `docs/BIZ-20260818-02-python-langgraph.md` 和 `docs/BIZ-20260818-03-python311-deepseek.md` 为准。

- 所有应用和测试代码使用 Python `3.11.9`；不得创建 Java/JVM、Spring 或 Spring AI 工程与实现，也不得在未记录决策的情况下更换 Python 版本。
- Agent 工作流统一使用 LangGraph 编排；未经新的 BIZ/DEV 决策，不替换为其他 Agent 框架，也不自研平行的工作流引擎。
- 模型供应商统一使用 DeepSeek；具体模型 ID、调用库和 API 参数由 DEV 冻结。未经新 BIZ/DEV 决策，不接入其他模型供应商或隐式 fallback。
- LangGraph 只负责应用层状态和流程编排。规则领域模型、JSON Schema 校验和确定性语义校验不得依赖 LangGraph 类型或具体模型 SDK。
- 包管理器、项目布局和依赖版本必须在 DEV 中明确并通过锁文件固定；本地开发、测试和 CI 必须使用 Python `3.11.9`，不得依赖未记录的全局 Python 包。

当前阶段以 `docs/进度文档.md` 和 `docs/BIZ-20260818-01-phase1-local-documents.md` 为准，范围冻结为：

- 先交付 **Phase 1.0 后端骨架**，再交付 **Rule Parsing Agent** 的最小可验证闭环。
- 后端骨架仅包含 FastAPI、集中配置、MongoDB 连接/健康检查和基础设施 Schema migration。
- 规则来源只使用仓库内或显式配置目录中的本地 Markdown 文档。
- 解析结果只能是待审核的结构化规则草稿、`requiredFacts`、测试案例和可追溯的来源信息，不能直接成为正式业务决策。

除非用户明确改变范围并先更新相关 `REQ`/`BIZ`/`DEV`，当前阶段禁止实现：

- Wiki API、Wiki 同步、页面修订同步、RAG、向量库或知识库问答；
- Agent 2、数据库元数据发现、Text-to-SQL、SQL 模板生成或 SQL 执行；
- 事实注册中心、正式规则发布、MongoDB 版本库、规则执行引擎和决策原因树；
- 销售查询 Agent、业务诊断链路、生产流程集成和业务 UI。

发现上述需求时，只记录到对应文档的“后续范围”或进度遗留项中，不创建占位实现、模拟集成或为未来扩展预建基础设施。

## 4. 任务开始前的必做检查

- 阅读当前进度和任务关联文档，确认任务属于当前阶段且验收标准可验证。
- 在 PRD 和技术设计中搜索相关功能名、领域术语、输入输出、外部依赖和安全约束。
- 检查受影响代码、配置、测试和样例文件；沿用既有模式，不凭空发明第二套约定。
- 新功能或较大改造若缺少 `REQ`，复杂实现若缺少 `DEV`，先补文档再编码。
- 如果任务会改变产品范围、架构、数据结构或实施顺序，先更新对应文档并记录决策，再修改代码。
- 仓库尚无对应实现模式时，先在 `DEV` 中明确最小方案、依赖和取舍，再创建工程骨架。

## 5. 文档治理

所有项目文档使用 UTF-8 Markdown，放在 `docs/` 下，并使用相对链接建立追溯关系。

| 文档类型 | 命名格式 | 使用时机 |
| --- | --- | --- |
| `REQ` | `REQ-YYYYMMDD-XX-*.md` | 新功能或大范围改造前，定义范围、非目标和验收标准 |
| `PROG` | `PROG-YYYYMMDD.md` | 当日进度日志，记录状态、证据、问题和下一任务 |
| `BUG` | `BUG-YYYYMMDD-XX-*.md` | 发现缺陷时立即记录复现、影响和修复验收 |
| `BIZ` | `BIZ-YYYYMMDD-XX-*.md` | 业务边界、流程或实施策略发生确认或调整时 |
| `DEV` | `DEV-YYYYMMDD-XX-*.md` | 复杂模块、架构调整或分阶段实现前 |

关联规则：

- `PROG` 必须引用相关 `REQ`/`BUG`，并写明完成证据、遗留问题和下一任务。
- `BUG` 必须引用来源 `REQ`；无法确定来源时写明原因，不能伪造关联。
- `BIZ` 必须引用对应 `REQ`，并记录被替代的旧决策（如有）。
- `DEV` 必须引用对应 `REQ`，实现偏离 `DEV` 时先更新文档并说明原因。
- 同一信息只保留一个权威位置；其他文档用链接引用，避免多份副本逐渐不一致。

## 6. Vibe Coding 实施方式

- 一次只完成一个最小纵向切片：文档定位 → 契约/Schema → 领域逻辑 → 必要适配器 → 测试 → 文档与进度。任务不涉及的层次直接跳过。
- 当前阶段先定义本地规则文档格式、结构化输出 Schema、语义校验和样例，再接入模型或应用入口。
- LangGraph 的 state、node、edge、条件分支和错误流必须先在 `DEV` 中定义；graph node 应保持单一职责，副作用通过适配器完成。
- 每个切片必须能通过自动化测试或可重复命令独立验证；不要并行铺开多个半成品模块。
- 优先复用成熟依赖；引入新依赖前检查许可证、维护状态、必要性和与现有技术栈的兼容性，并在 `DEV` 中说明理由。
- 未经当前 `REQ` 要求，不新增数据库、消息队列、缓存、容器编排或独立服务。
- FastAPI lifespan 统一持有和释放 MongoDB 等进程级资源；禁止在模块导入、请求处理器或领域代码中临时创建数据库 Client。
- 配置只能通过集中 Settings 对象读取；API、日志和 CLI 不得输出完整 MongoDB URI 或 DeepSeek Key。
- MongoDB migration 必须版本化、幂等且 fail-fast；当前只允许创建 `schema_migrations` 和 `app_metadata` 基础设施集合。

## 7. 规则解析 Agent 的工程边界

- Rule Parsing Agent 只负责“理解并结构化规则”，输出状态必须是草稿；它不得发布规则、执行规则或改变业务流程状态。
- 模型输出一律视为不可信输入。必须经过 JSON Schema 校验和确定性的语义校验；校验失败时返回明确错误，不得自动补成看似有效的规则。
- 解析结果必须可追溯到源文件，至少保留相对路径、内容哈希和解析器/Prompt 版本；不得仅保存无法复核的自然语言摘要。
- 领域模型和校验逻辑不得依赖 LangGraph、具体模型 SDK、文件系统或数据库。
- 本地文件读取、模型调用和结果持久化（若后续需求需要）属于适配器；API/CLI 不直接承载解析规则或供应商调用细节。
- DeepSeek 调用必须封装在模型适配器中；业务和 LangGraph 路由不得直接依赖 DeepSeek SDK/HTTP 响应类型、模型名称或凭据。
- LangGraph state 使用显式、可类型检查的结构；节点之间只传递用例需要的数据，不把 SDK Client、文件句柄、异常对象或密钥放入 state。
- 配置统一加载和校验，不在业务代码中散读环境变量；密钥只能来自运行环境或本地未跟踪配置。
- 本地文档读取必须限制在配置的根目录内，拒绝路径穿越；空文件、不可读文件、不支持的编码或格式必须显式报错。
- 模型 Provider 的超时、限流、空响应、非 JSON、Schema 不匹配和语义冲突都必须有可测试的失败路径；重试必须有上限且不能产生重复副作用。
- DeepSeek 凭据只能从运行环境或本地未跟踪配置注入；不得写入源码、Prompt、测试夹具、日志或 LangGraph state。
- 不为了未来 Wiki 接入提前实现 Wiki Client、RAG 抽象、向量索引或通用多数据源框架。

## 8. 测试要求

- 先写或同步验收样例，再实现代码；规则解析至少覆盖正常、反向、边界、嵌套条件、例外条件、缺失字段、歧义文本和非法模型输出。
- Schema 校验和语义校验使用确定性单元测试；模型调用通过可替换端口或测试替身隔离。
- LangGraph node、条件路由、失败分支和最终 state 必须可独立测试；不得只依赖端到端真实模型测试。
- 默认测试套件不得依赖真实模型、网络、个人目录、时间随机性或密钥；真实 Provider 测试必须显式启用。
- 修复缺陷时先添加能复现问题的失败测试，再修复并保留回归测试。
- 先运行与改动最接近的测试，再运行受影响模块的完整测试；在 `PROG` 中记录命令和结果。

## 9. 完成标准

任务只有同时满足以下条件才算完成：

- `REQ` 的相关验收标准全部满足，且未实现当前阶段明确排除的能力。
- 正常、失败、空输入和边界路径均有处理，相关自动化测试通过。
- 外部 Provider 的失败、超时、空数据和非法输出已被显式处理。
- 文档、样例、代码和测试保持一致；`docs/进度文档.md` 与当日 `PROG` 已更新状态、证据、遗留问题和下一任务。
- 变更范围仅包含当前任务，没有无关重构、生成物、密钥或本地数据。

“代码能运行”不等于完成；无法提供可重复验证证据时，任务仍未完成。

## 10. Git 与安全

- 未经用户明确授权，不提交、不推送远程、不创建 PR、不发布版本或部署环境。
- 不使用破坏性 Git 操作覆盖用户修改；发现工作区已有改动时，保留并绕开无关内容。
- 不提交 `.env*`、密钥、数据库、备份、日志、模型原始敏感输出、依赖目录和构建产物。
- 日志、测试夹具和错误信息不得包含密钥、访问令牌或未脱敏业务数据。
