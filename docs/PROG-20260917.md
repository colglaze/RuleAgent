# PROG-20260917：优化方案驱动的 3.1.0 新规则与完整交付（离线）

- 状态：`IMPLEMENTED_OFFLINE_AWAITING_REAL_WRITE_AUTHORIZATION`
- 前序记录：[PROG-20260907](PROG-20260907.md)
- 依据：[REQ-20260917-01](REQ-20260917-01-optimization-plan-rule-handoff.md)、
  [BIZ-20260917-01](BIZ-20260917-01-optimization-plan-authority-agent1.md)、
  [DEV-20260917-01](DEV-20260917-01-optimization-plan-rule-handoff.md)、
  [DEV-20260917-02](DEV-20260917-02-sqlbot-complete-delivery-intake.md)
- 外部权威：SqlBot `BIZ-20260917-03`、`DEV-20260917-02`

## 授权范围

本提示词授权本地代码、契约、文档修改及离线验证。**未授权**真实 MongoDB 写入、在线模型调用或
Git 推送。因此本次只完成离线实现与可审阅交付包，不运行 `persist_optimization_plan_v31_delivery.py`
对正式库的写入。

## 来源身份

固定私有 `RuleDataReferences`：

- commit `2240e5bd18e36d17650896a10cc61e1c18e3daa0`
- bundle digest `6d403f1a110ea1699a72aec76f38be944583670f96767f5e3f44b2ac2e565160`
- 优化方案文件 SHA-256 `c049af189fc3689bac8e96408d9e7239a8c70b66bbcd15829e509c6d524b648f`

新版本区分：文件字节哈希、§1.3+§5.1+§5.2 提取块哈希、提取器 `optimization-plan-sections-v1`。
旧 1402 字符块哈希 `f285643e…` 仍只解释 3.0.0 规则块，不是 catalogDigest。

## 交付内容

1. Schema 3.1.0 领域模型、求值器、FBR 导出器、用途门禁。
2. 报告与原始数据两个独立 catalog / 规则树 / ruleVersion / FBR 批次 / 案例。
3. Schema v6 完整闭包持久化（insert-only、同内容幂等、异内容冲突、中途失败不可消费）。
4. 新构建/持久化入口；旧 v5 脚本与 18 请求信任锚未改。
5. 覆盖矩阵与契约差异文档。
6. 私有交付包写入 `RuleDataReferences-recovery/optimization-plan-v31-20260917/`（公开仓库外）。

应用版本 `0.13.0`。优化方案 profile 的 parserVersion 冻结为同一常量，不把历史 3.0.0 产物改写。

## 状态分列

| 层面 | 状态 |
| --- | --- |
| 离线实现（代码/契约/测试/文档） | 完成 |
| 业务公式验收（合成数据离线求值） | 完成；不等于生产执行 |
| 真实落库 | 未授权，未执行 |
| SqlBot 3.1.0 接入 | 仅接口文档；生产代码未改 |

## 验证命令与结果

见本文件末尾“实测记录”。默认测试不读取私有源文件、不连生产库、不调用模型。

## 剩余阻断

- 真实 MongoDB Schema v6 写入需单独授权。
- SqlBot 必须独立升级完整交付读取、3.1.0 FBR、运行参数与集合量词；在此之前不能生成总 SQL。
- 物理表列/JOIN/grant 仍属 metadataReview。
- 未跟踪的 `tests/audit/test_round3_runtime_falsification.py` 仅补了 UTF-8 打开，断言未改。

## 实测记录（2026-09-17）

来源身份（只读，未改 git config）：

- `git -c safe.directory=... rev-parse HEAD` = `2240e5bd18e36d17650896a10cc61e1c18e3daa0`
- bundle digest = `6d403f1a110ea1699a72aec76f38be944583670f96767f5e3f44b2ac2e565160`
- 优化方案文件 SHA-256 = `c049af189fc3689bac8e96408d9e7239a8c70b66bbcd15829e509c6d524b648f`
- 提取块 parseInputSha256 = `aebdbf4cf89d469ccd2bfc61d9f70aa24d4361e3ed5fb947c1221f8f8676b662`
  （≠ 文件哈希，≠ 旧 1402 字符块 `f285643e…`）

离线命令：

- `python -m scripts.export_contract_schemas`
- `python -m pytest -q` → **232 passed, 6 deselected**
- `ruff check src tests scripts` → All checks passed
- `ruff format --check src tests scripts` → 137 files already formatted
- `mypy` → Success, 80 source files
- `pip check` → No broken requirements found
- `git diff --check` → 无空白错误

私有交付包（公开仓库外）
`D:\Python\pyWorkspace\RuleDataReferences-recovery\optimization-plan-v31-20260917\`：

| ruleSet | ruleVersion | catalogDigest 前缀 | 请求 | 案例 |
| --- | --- | --- | --- | --- |
| 报告 | `REPORT_RELEASE_ALL_001@20260917T140000000000Z-c049af189fc3-74a69e146b34` | `74a69e146b34` | 33 | 62 |
| 原始数据 | `RAW_DATA_RELEASE_ALL_001@20260917T140000000000Z-c049af189fc3-aac9288cc74f` | `aac9288cc74f` | 18 | 18 |

两个版本均 `draft` / `executable=false` / `purpose=optimization-plan-generation`。来源前缀 `c049af189fc3` 关闭到**文件哈希**，不是旧块哈希。

未执行：`persist_optimization_plan_v31_delivery.py`、DeepSeek、SqlBot 生产代码、Git 提交/推送。
