# PROG-20260901：私有字段映射与来源资料登记

- 日期：2026-09-01
- RuleReader 版本：`0.10.0`
- MongoDB Schema：v3（未改变）
- 关联需求：
  [SqlBot REQ-20260828-03](https://github.com/colglaze/SqlBot/blob/main/docs/requirements/REQ-20260828-03-local-candidate-evidence-integration.md)
- 前置记录：[PROG-20260827](PROG-20260827.md)

## 本次完成

- 将用户指定目录内 17 个项目交付参考文件收拢到独立私有
  [colglaze/RuleDataReferences](https://github.com/colglaze/RuleDataReferences)；
- 原始工作簿、视图定义、规则原文和 Word 设计文件只在私有仓库保存，RuleReader 不复制其内容；
- 私有仓库逐文件保留 SHA-256、字节数和来源修改时间，并提供不回显匹配值的密钥模式检查；
- RuleReader 仅登记 bundle ID、私有 commit 和 content digest；
- 更新 `README.md` 与 `docs/进度文档.md`，移除当前文档中的个人绝对来源路径；
- 没有修改应用代码、契约、数据库迁移、规则版本或事实交接记录；
- 保留并绕开用户已有 `.obsidian/workspace.json` 修改。

## 固定身份

- Bundle：`PROJECT_RELEASE_REFERENCE_20260901_001`；
- 私有 commit：`2240e5bd18e36d17650896a10cc61e1c18e3daa0`；
- Content digest：`6d403f1a110ea1699a72aec76f38be944583670f96767f5e3f44b2ac2e565160`；
- 来源文件：17 个、433,997 字节；
- 字段映射工作簿 SHA-256：
  `872706f07792a618888a13d8944d7c59f454e5554388fa2dece511abebb30242`。

## 信任边界

- 私有资料固定 `authority=none`、`executable=false`；
- 不进入 RuleReader 运行时，不覆盖不可变规则或 `fact_binding_handoffs`；
- 不授予 SqlBot 关系、列、join 或执行权限，也不能清除 blocking uncertainty；
- 未连接 MongoDB、SQL Server 或在线模型，未读取或输出凭据。

## 验证证据

- 私有 bundle 完整性、逐文件哈希、Git staged byte identity、密钥模式与 JSON Schema：通过；
- RuleReader `.venv\Scripts\ruff.exe check .`：通过；
- RuleReader `.venv\Scripts\ruff.exe format --check .`：通过；
- RuleReader `.venv\Scripts\pytest.exe -q`：83 passed, 3 deselected；
- RuleReader `git diff --check`（排除受保护的 `.obsidian/workspace.json`）：通过。

## 下一任务

RuleReader 当前没有进行中的实现任务。业务复审、修订草稿持久化和 SqlBot Phase 5 均需要新的明确
授权；私有资料归档本身不推进这些阶段。
