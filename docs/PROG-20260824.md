# PROG-20260824：事实绑定契约 2.0、V2-only、真实规则与不可变 MongoDB 交接

- 日期：2026-08-24
- 当前阶段：Phase 1.10 已完成，真实 Schema 2.0 草稿的 33 条事实请求已不可变交接
- 权威进度：[进度文档](进度文档.md)
- 当前契约需求：[REQ-20260819-01](REQ-20260819-01-rule-contract-v2-agent2-handoff.md)
- 当前业务决策：[BIZ-20260819-01](BIZ-20260819-01-agent2-handoff-baseline.md)
- 当前技术方案：[DEV-20260819-01](DEV-20260819-01-rule-contract-v2.md)
- MongoDB 交接需求：[REQ-20260824-01](REQ-20260824-01-mongodb-fact-binding-handoff.md)
- MongoDB 交接决策：[BIZ-20260824-01](BIZ-20260824-01-mongodb-fact-binding-handoff.md)
- MongoDB 交接方案：[DEV-20260824-01](DEV-20260824-01-mongodb-fact-binding-handoff.md)
- 待关闭缺陷：[BUG-20260819-01](BUG-20260819-01-rule-ast-semantic-loss.md)
- 上次实施记录：[PROG-20260819](PROG-20260819.md)

## 1. 新会话阅读顺序

1. 根目录 `AGENTS.md`；
2. [进度文档](进度文档.md)；
3. 本交接文档；
4. 与所选任务直接相关的 REQ/BIZ/DEV/BUG；
5. [PRD](PRD.md)、[技术设计](技术设计文档.md)、[实施文档](实施文档.md)；
6. 受影响代码和测试。

文档已有答案时不要重复询问。若新任务改变范围、架构、数据结构或实施顺序，必须先更新对应 REQ/BIZ/DEV。

## 2. 当前确定状态

- 工作目录：`<RuleReader checkout>`；
- Python `3.11.9`，应用版本 `0.8.0`；
- FastAPI、LangGraph `1.2.11`、DeepSeek `deepseek-v4-flash`、MongoDB Schema v3；
- 新解析固定输出规则 Schema `2.0.0`、`status=draft`、`executable=false`；
- Schema `1.0.0` 仅只读兼容，不自动迁移，也不能导出事实绑定请求；
- RuleReader `main` 的已交付基线为提交 `44731b6`，已推送到 `origin/main`；
- Phase 1.8/1.9/1.10 工作区变更尚未提交或推送；
- 工作区另有用户原有的 `.obsidian/workspace.json` 修改，必须保留且不得纳入后续无关提交。

## 3. 已交付能力

- 受控本地 Markdown 读取、来源哈希和版本元数据；
- DeepSeek JSON Output 适配器及 LangGraph 有限重试流程；
- Schema `2.0.0` 表达式/条件 AST、四类事实、粒度、参数、空值策略和派生表达式；
- 事实引用闭包、派生环、类型/操作符、映射完整性和测试案例确定性执行；
- 显式不可变 draft 保存与精确版本回读；
- `GET /api/v1/rules/versions/{ruleVersion}/fact-binding-requests`；
- 默认和显式 `contractVersion=2.0.0` 均只导出补齐实体、字段、筛选、聚合、时间范围、结果、来源证据和不确定性的新版；
- `contractVersion=1.0.0` 与未知版本均返回 422，V1 只保留静态历史兼容夹具；
- Draft 2020-12 静态 Schema、合法未决/非法漏字段/旧版样例和 Pydantic + `jsonschema` 双重验证。

RuleReader 不生成 SQL、不读取目标数据库元数据，也不实现 Agent 2。

RuleReader 现在还拥有 `fact_binding_handoffs`：只有显式 CLI 能从已持久化 `RuleParseResultV2` 创建不可变记录；SqlBot 只读。交接成功不解除任何 blocking uncertainty，也不表示规则通过业务审核。

## 4. 当前数据与来源

正式 `rule_versions` 当前有两条不可变草稿。历史 Schema `1.0.0`：

- 版本：`REPORT_RELEASE_ALL_001@20260818T092511165521Z-562eabd40e5a`；
- 状态：draft、不可执行；
- 因 AST 语义丢失禁止进入 Agent 2，且不得覆盖或删除。

新 Schema `2.0.0`：

- 版本：`REPORT_RELEASE_ALL_001@20260824T080726492666Z-562eabd40e5a`；
- 状态：draft、不可执行，来源为受控 `reviewed_import`；
- 41 个事实、65 个条件、20 个案例，导出 33 条带阻断不确定性的 V2 事实请求；
- 等待业务审核，阻断项解决前不得进入 SqlBot SQL 候选生成。

正式 `fact_binding_handoffs` 当前仅包含上述 V2 规则的 33 条不可变记录：

- request ID 与 fact code 均为 33 个唯一值，`contract_version` 仅为 `2.0.0`；
- 33 条均通过 Pydantic、仓库内 Draft 2020-12 Schema、安全文本、canonical SHA-256 和身份复核；
- 33 条均原样保留 `blocking` uncertainty；历史 V1 规则对应记录为 0；
- 首次统一 `created_at=2026-08-24T09:02:47.695000+00:00`，幂等重放不更新时间。

本次关闭 BUG 使用的同一来源：

```text
RuleDataReferences/sources/project-release-rules/规则agent规划/报告和数据的释放规则/项目报告释放规则.md
```

其规范化 SHA-256 已于 2026-08-24 复核为：

```text
562eabd40e5a5701fb9515499b542a6e2ff46056464621b1abb0a7c37f116e4d
```

另有 `wiki现规则\项目报告释放规则.md`，哈希为 `ec70dd78...`，内容不同；未经用户确认不得替换 BUG 来源。原始数据规则当前位于同一 `规则agent规划\报告和数据的释放规则` 目录，哈希为 `29711eb8...`。

## 5. 最近验证基线

提交 `44731b6` 完成时：

- Ruff 通过；
- 严格 Mypy 检查 34 个源码文件通过；
- 默认测试：36 passed、2 deselected；
- MongoDB integration：1 passed、37 deselected；
- `pip check` 通过；
- 未调用真实 DeepSeek，未修改正式 MongoDB 数据。

2026-08-24 Phase 1.8 验证：

- RuleReader `HEAD` 与 `origin/main` 均为 `44731b6`；
- 相关契约/API 测试：25 passed；
- 默认离线测试：44 passed、2 deselected；
- MongoDB integration：1 passed、45 deselected，随机测试库已删除；
- Ruff、严格 Mypy（35 个源码文件）和 `pip check` 通过；
- 当前 SqlBot 进程级只读兼容复核：`1.0.0` 样例可解析，`2.0.0` 按现有严格输入模型被拒绝，符合尚未升级 intake 的预期；
- 没有调用 DeepSeek、没有改写正式 `rule_reader` MongoDB 数据、没有访问任何 SQL 数据库，也没有实现 Agent 2、Text-to-SQL 或 SQL 执行。

2026-08-24 Phase 1.9 当前结果：

- 用户明确替代 Phase 1.8 的过渡策略，RuleReader 运行时禁止继续输出 `FactBindingRequest 1.0.0`；应用升级到 `0.6.0`。
- HTTP 默认/显式 V2、显式 V1 拒绝、未知版本拒绝、静态正反样例、历史兼容模型和导出双重验证均有离线回归。
- Prompt 迭代到 `rule-parser-v6`，明确点分 `factCode`、非空查询参数、`allowedValues=[]`、派生表达式、允许的 expression/condition kind、案例覆盖和禁止 SQL/凭据。
- 校验重试最多返回 100 条字段路径，并把上一候选作为 `previousCandidate` 交给同一 DeepSeek 修正；可信代码从不补候选字段，修正版完整重验。
- 首轮真实调用使用 `deepseek-v4-flash`、同一 7,501 字符来源和哈希 `562eabd4...`；受限工作流（首个候选加最多 2 次修正）仍返回 `CANDIDATE_SCHEMA_INVALID`。
- 首轮最终失败路径包括 `allowedValues` 非数组、`derived` 缺少 `derivation` 和契约外条件节点；专项 AST/分支/案例审计与 MongoDB 保存均未开始，因而没有新增不可变版本。
- 用户随后追加授权“最多三次”。进程内计数硬上限确认恰好执行 3 次 Provider 请求；仍使用 `rule-parser-v6`、同一模型、同一 7,501 字符来源和完整哈希。
- 追加轮最终仍为 `CANDIDATE_SCHEMA_INVALID`：大量 `requiredFacts.factCode` 以及 `derivation`、`rootCondition`、`fieldMappings` 中对应引用不满足点分小写正则 `^[a-z][a-z0-9_]*(?:\.[a-z][a-z0-9_]*)+$`。解析在专项审计、事实绑定验证和持久化之前停止。
- 最终离线测试为 `46 passed, 2 deselected`；MongoDB 隔离集成为 `1 passed, 47 deselected`；Ruff、严格 Mypy（35 个源码文件）和 `pip check` 通过。
- 追加轮结束后再次只读复核正式库：`rule_versions` 总计 1 条，Schema `1.0.0` 为 1 条，Schema `2.0.0` 为 0 条，历史目标版本恰好 1 条。
- 未访问任何 SQL 数据库，未生成/执行 SQL，未实现 Agent 2 或 Text-to-SQL；历史 Schema `1.0.0` 草稿未修改。

2026-08-24 Phase 1.9 受控关闭结果：

- 用户明确同意不再调用 DeepSeek 的受控 `reviewed_import`。该路径已同步到 REQ/BIZ/DEV；它不进入公开 API，不接入新的运行时模型 Provider，也不冒充 DeepSeek 输出。
- 应用升级到 `0.7.0`；Parser 元数据为 `provider=reviewed_import`、`promptVersion=reviewed-import-v1`、`model=codex-gpt-5`。来源仍为同一 7,501 字符文档和 SHA-256 `562eabd40e5a5701fb9515499b542a6e2ff46056464621b1abb0a7c37f116e4d`。
- reviewed profile 形成 41 个事实、65 个条件和 20 个确定性案例。33 个事实为 `source/aggregate/exists`，8 个为带确定性 `derivation` 的 `derived`；8 个来源字段为目录精确候选，其余保持 `unresolved`。
- 专项审计验证 `R+0.1>=F` 的 `add/fact/literal/gte`、`F=B+E+C+M`、`T=E+C+M`、普通产品分支、759 两个互斥分档、显式 `NOT` 未结束流程、直接放行分支、合并去重及 60/75/180 天日期相等；六类案例全部由解释器通过。
- V2 最终规则不再携带目录 `sourceExpression`；候选与导出载荷还经过可执行查询文本、数据库连接串、账号和密码模式检查。
- 不可变版本 `REPORT_RELEASE_ALL_001@20260824T080726492666Z-562eabd40e5a` 于 `2026-08-24T08:07:33.201000+00:00` 写入，随后精确回读与写入前对象完全一致。正式库现为总计 2 条、Schema `1.0.0` 1 条、Schema `2.0.0` 1 条。
- 回读版本重新导出 33 条 `FactBindingRequest 2.0.0`，与写入前导出完全一致，并逐条通过 Pydantic、独立 Draft 2020-12 Schema 和安全文本门禁。33 条均含 `blocking` 不确定性，SqlBot 不得开始生成 SQL。
- 正式 FastAPI 生命周期只读验收：精确版本回读 200 且 `schemaVersion=2.0.0`；默认事实请求 200、数量 33、版本集合仅含 `2.0.0`；显式 `contractVersion=1.0.0` 返回 422。
- 本地导出位于 Git 忽略的 `artifacts/report-release-all-001/`：规则 JSON SHA-256 为 `993a527f94fb38281d9804cee3f507c9b25cdcaf7600e1af50df805f3ff57fdd`，事实请求 JSON SHA-256 为 `7e8d52f0373500c69d8cc17302568cdaabf83a31244738f4fddb9d3cccee0f91`。
- 最终默认离线测试为 `52 passed, 2 deselected`；MongoDB 隔离集成为 `1 passed, 53 deselected`；Ruff、严格 Mypy（37 个源码文件）和 `pip check` 通过。
- 未再次调用 DeepSeek，未访问任何 SQL 数据库，未生成或执行 SQL，未实现 Agent 2、目标库元数据、批准、发布或规则执行。

2026-08-24 Phase 1.10 MongoDB 不可变交接结果：

- 新建 [REQ-20260824-01](REQ-20260824-01-mongodb-fact-binding-handoff.md)、[BIZ-20260824-01](BIZ-20260824-01-mongodb-fact-binding-handoff.md) 和 [DEV-20260824-01](DEV-20260824-01-mongodb-fact-binding-handoff.md)，冻结 RuleReader 唯一写入、SqlBot 只读的集合所有权、不可变性、幂等性和索引。
- 应用升级为 `0.8.0`，MongoDB Schema v3 新增 `fact_binding_handoffs`。索引为内建 `_id_`、`uq_fact_binding_handoffs_request_id` 唯一索引和 `uq_fact_binding_handoffs_rule_version_fact_code` 唯一联合索引。
- 新增显式 `rule-reader persist-handoffs --rule-version <version>`；它只精确回读 `rule_versions`，不会读取源 Markdown、构造 DeepSeek 适配器或修改规则记录。
- canonical payload 固定为 camelCase、排序键、无非语义空白、保留 Unicode 且禁止 NaN/Infinity 的 UTF-8 JSON；MongoDB 包装保存 request/rule/fact/contract 身份、SHA-256、首次 UTC 时间和完整 payload。
- 正式写入目标 `REPORT_RELEASE_ALL_001@20260824T080726492666Z-562eabd40e5a`：首次 `records=33, inserted=33, existing=0`；立即重放 `records=33, inserted=0, existing=33`。两次均报告 `blockingRequests=33`、`sourceRuleUnchanged=true`。
- 最终只读审计：33 个 request ID、33 个 fact code；Pydantic、静态 Schema、安全门禁、canonical hash 和身份校验均为 `33/33`；V1 交接为 0；v3 migration 恰好一条。
- 正式 `rule_versions` 总数前后均为 2。历史 V1 BSON SHA-256 前后均为 `546dab2823449e626f546024452f20b9b078804e90110afdaff09e5d6fb89a81`，目标 V2 前后均为 `e13eea6befc0591fd29a478a96cfb17ced3f9e5b7447caf1032e1da9bf18dd36`。
- 验证命令与结果：`pytest -q` 为 `57 passed, 3 deselected`；`pytest -q -m integration tests/integration/test_mongodb.py tests/integration/test_fact_binding_handoffs_mongodb.py` 为 `2 passed`；`ruff check .`、严格 `mypy`（42 个源码文件）和 `pip check` 均通过。
- 全过程未调用 DeepSeek、未重新解析来源、未访问 SQL Server、未生成 SQL、未输出或保存连接串/凭据，也未修改 `.obsidian/workspace.json`。

## 6. 独立 SqlBot 状态

- 工作目录：`<SqlBot checkout>`；
- 当前 `main` 与 `origin/main` 同步，提交为 `37a659a`；
- 事实绑定输入契约已完成；随后已完成确定性规则 canonicalization、哈希和结构化 diff；
- 当前 intake 只支持 `FactBindingRequest 1.0.0`，尚不能消费本次新增的 `2.0.0`；
- 其 `docs/progress/PROG-20260820.md` 记录 29 个测试通过；
- DeepSeek SQL 生成、SQL AST、安全验证和审核仍未实现。

SqlBot 的后续开发必须在 SqlBot 仓库建立或更新其自己的 REQ/BIZ/DEV，不能混入 RuleReader。

## 7. 下一任务选项

### A. 审核已持久化 Schema 2.0 草稿

业务人员按精确版本审核 41 个事实、65 个条件、20 个案例和 8 个候选映射。该动作不覆盖旧版、不修改不可变草稿；如需修正，必须形成新的来源或新的不可变版本。

### B. 治理 DeepSeek 重试、审计与幂等性

先新建 REQ/BIZ/DEV，再实现退避、尝试次数、Provider request ID、token usage 或请求幂等键。不要与 A 同时展开。

### C. 升级 SqlBot 的 FactBindingRequest 2.0 intake

切换到 SqlBot 仓库，先建立其独立 REQ/BIZ/DEV，只实现对 RuleReader `fact_binding_handoffs` 的只读 V2 intake、新版 JSON Schema/来源/哈希/不确定性校验和确定性 `blocked` 门禁。任一 `blocking` 项不得进入候选生成；SqlBot 不得修改交接集合，RuleReader 不再提供旧版降级。本任务不生成 SQL。

### D. 推进 SqlBot SQL 候选生成

切换到 SqlBot 仓库，先读取其最新 AGENTS/PROG/REQ/BIZ/DEV，再建立 DeepSeek provider 与候选生成切片。RuleReader 不承载该实现。

## 8. 安全门禁

- 默认测试不得调用真实 DeepSeek；真实调用必须由用户明确授权，并会消耗额度；
- `persist=true` 或 CLI `--persist` 必须单独明确授权，不能因解析成功自动保存；
- `persist-handoffs` 同样必须显式提供精确已持久化规则版本；它不得接受内存候选、源文件或 Schema `1.0.0`，哈希冲突不得覆盖；
- 不输出或提交 `.env`、DeepSeek Key、MongoDB URI、模型原始敏感响应或业务正文；
- 不覆盖历史草稿，不修改或提交 `.obsidian/workspace.json`；
- 不在 RuleReader 内实现 SQL、目标库元数据、审批、发布或执行。
- RuleReader 已按用户明确决定切换为 V2-only；SqlBot 未升级期间保持跨仓库不兼容，不能临时恢复 V1 输出绕过门禁。

## 9. 可直接复制到下一对话

```text
请继续处理 `<RuleReader checkout>`。

先完整阅读根目录 AGENTS.md、docs/进度文档.md 和 docs/PROG-20260824.md，再按链接阅读本次任务直接相关的 REQ/BIZ/DEV/BUG、PRD、技术设计、实施文档、代码和测试。保留并绕开 .obsidian/workspace.json 的已有修改，不要读取或输出任何密钥。

当前应用版本为 0.8.0，Python 3.11.9，MongoDB Schema v3；Phase 1.0-1.10 已完成。真实 Schema 2.0.0 草稿 REPORT_RELEASE_ALL_001@20260824T080726492666Z-562eabd40e5a 已受控导入、不可变保存并精确回读；33 个非派生事实已经 Pydantic、静态 JSON Schema、安全和 canonical hash 门禁写入 RuleReader 所有、SqlBot 只读的 fact_binding_handoffs，且全部因未决查询语义保持 blocking。相同规则重放插入 0 条；历史 Schema 1.0.0 草稿与两个 rule_versions BSON 未修改。

我的新任务是：<选择 A/B/C/D 之一并写明具体目标>。

如果选择 A，只做业务审核和问题记录，不把草稿标记为已发布或可执行。如果选择 C，必须在 SqlBot 独立 REQ/BIZ/DEV 中以只读方式升级 V2 intake，不得修改 RuleReader 集合，并让任一 blocking 不确定性停止候选生成。若任务改变范围或架构，先更新 REQ/BIZ/DEV；默认测试不要调用真实 DeepSeek。
```
