# RuleReader 进度文档

> 2026-09-05 仓库核验：提交 `65a9684` 缺失的 V3 公开交付已根据可达设计和当前用户授权重建；
> 历史真实模型响应、私有资料和旧测试计数仍不作为恢复验收证据。当前证据见
> [BUG-20260905-01](BUG-20260905-01-incomplete-v3-commit.md) 和 [PROG-20260905](PROG-20260905.md)。

> 2026-09-07 文档纠偏（仅改文档，不改应用逻辑）：①[BUG-20260906-05](BUG-20260906-05-agent1-v2-real-call-convergence-blocker.md)
> 文件标题编号已修正；5 份历史运行审计日志未留存到本机可审计位置（`artifacts/v2-agent1-real-runs-20260906/`
> 在当前工作区不存在），其失败面演化属当日会话记录、"模型重复输出"根因属分析推断，反馈传递、
> 幂等缓存与适配器环节未被系统性排除，证据缺口已在该缺陷文档显式记录。②"结构/案例两阶段"仅为
> 设计方向，未实现、未经真实 Provider 验证，不保证收敛或消除输出截断（见
> [DEV-20260902-02](DEV-20260902-02-agent1-v3-prompt.md) 验证状态边界）。③MongoDB 状态以
> [PROG-20260907](PROG-20260907.md) 为准：2026-09-06 快照仅描述换机重建实例（业务集合 0 条、
> 基础设施 5 条文档），本机原始正式库历史数据从未丢失，且已于 2026-09-07 完成 V3 Schema v5
> 落库；文档中的历史记录不构成数据库备份。④Git 状态以 2026-09-07 实测为准（见"遗留问题"
> Git 两条）。纠偏明细见 [PROG-20260907](PROG-20260907.md) 追加章节。

- 更新时间：2026-09-07
- 当前阶段：`Post-Phase 1.10 - V3 confirmed 交付已按用户授权持久化到本机正式库（Schema v5，rule_versions_v3 1 条 + 单文档 batch 18 条请求，executable=false 待审核）；V3 恢复候选已恢复 1 条`
- 当前任务：`等待用户决策：BUG-20260906-05 关闭路径（继续有界重试 / 两阶段解析改造 / 沿用 reviewed_import）；配置两个 loopback 环境变量后运行隔离 integration`
- 已完成阶段：`Phase 0 - Documentation Baseline`、`Phase 1.0 - Backend Skeleton`、`Phase 1.1-1.5 - Rule Parsing Vertical Slice`、`Phase 1.6 - Immutable Draft Version Persistence`、`Phase 1.7 - Rule Contract v2 and Agent 2 Handoff`、`Phase 1.8 - Verifiable Fact Binding Contract`、`Phase 1.9 - Schema 2 Only and Reviewed Import`、`Phase 1.10 - Immutable MongoDB Fact Binding Handoff`
- 基线 REQ：[REQ-20260818-01](REQ-20260818-01-vibe-coding-bootstrap.md)
- 已完成实施 REQ：[REQ-20260818-02](REQ-20260818-02-backend-skeleton.md)
- 已实施 DEV：[DEV-20260818-01](DEV-20260818-01-backend-skeleton.md)
- 已完成实施 REQ：[REQ-20260818-03](REQ-20260818-03-rule-parser.md)
- 已实施 DEV：[DEV-20260818-02](DEV-20260818-02-rule-parser.md)
- 已完成实施 REQ：[REQ-20260818-04](REQ-20260818-04-rule-version-persistence.md)
- 已实施 DEV：[DEV-20260818-03](DEV-20260818-03-rule-version-persistence.md)
- 已完成实施 REQ：[REQ-20260819-01](REQ-20260819-01-rule-contract-v2-agent2-handoff.md)
- 已实施 DEV：[DEV-20260819-01](DEV-20260819-01-rule-contract-v2.md)
- 已完成实施 REQ：[REQ-20260824-01](REQ-20260824-01-mongodb-fact-binding-handoff.md)
- 已实施 DEV：[DEV-20260824-01](DEV-20260824-01-mongodb-fact-binding-handoff.md)
- 已完成修订 REQ：[REQ-20260827-01](REQ-20260827-01-schema2-business-review-remediation.md)
- 已实施修订 DEV：[DEV-20260827-01](DEV-20260827-01-schema2-business-review-remediation.md)
- 已完成治理 REQ：[REQ-20260827-02](REQ-20260827-02-deepseek-retry-audit-idempotency.md)
- 已实施治理 DEV：[DEV-20260827-02](DEV-20260827-02-deepseek-retry-audit-idempotency.md)
- 已批准 Slice 1/2 需求：[REQ-20260902-01](REQ-20260902-01-rule-contract-v3-agent2-ready-handoff.md)
- 已实施 Slice 1/2 方案：[DEV-20260902-01](DEV-20260902-01-rule-contract-v3-agent2-ready-handoff.md)
- 已批准离线规则结构提示词：[DEV-20260902-02](DEV-20260902-02-agent1-v3-prompt.md)
- V3 Agent 2 readiness 需求：[REQ-20260905-02](REQ-20260905-02-v3-agent2-handoff-readiness.md)
- V3 Agent 2 交接决策：[BIZ-20260905-02](BIZ-20260905-02-v3-agent2-handoff-contract.md)
- V3 Agent 2 readiness 设计：[DEV-20260905-02](DEV-20260905-02-v3-agent2-handoff-readiness.md)
- 16 项 blocking 业务确认裁决：[BIZ-20260906-01](BIZ-20260906-01-v3-blocker-business-confirmation.md)
- Slice 3-6 实现设计：[DEV-20260906-01](DEV-20260906-01-rule-parse-result-v3.md)
- V3 Schema v5 持久化需求：[REQ-20260906-01](REQ-20260906-01-v3-mongodb-persistence.md)
- V3 Schema v5 持久化业务决策：[BIZ-20260906-02](BIZ-20260906-02-v3-mongodb-persistence.md)
- V3 Schema v5 持久化设计：[DEV-20260906-02](DEV-20260906-02-v3-mongodb-persistence.md)
- confirmed profile 版本耦合缺陷（已修复）：[BUG-20260906-02](BUG-20260906-02-confirmed-profile-version-coupling.md)
- 已关闭的 V3 交接阻断：[BUG-20260905-02](BUG-20260905-02-v3-agent2-readiness-blockers.md)
- 已修复的换机环境与文档漂移：[BUG-20260906-01](BUG-20260906-01-reinstall-environment-and-v3-document-drift.md)
- 范围 BIZ：[BIZ-20260818-01](BIZ-20260818-01-phase1-local-documents.md)
- 技术栈 BIZ：[BIZ-20260818-02](BIZ-20260818-02-python-langgraph.md)
- 运行时与模型 BIZ：[BIZ-20260818-03](BIZ-20260818-03-python311-deepseek.md)
- 后端基础设施 BIZ：[BIZ-20260818-04](BIZ-20260818-04-backend-fastapi-mongodb.md)
- 规则解析 BIZ：[BIZ-20260818-05](BIZ-20260818-05-rule-json-version-mapping.md)
- 草稿版本归档 BIZ：[BIZ-20260818-06](BIZ-20260818-06-draft-rule-version-storage.md)
- 双 Agent 交接 BIZ：[BIZ-20260819-01](BIZ-20260819-01-agent2-handoff-baseline.md)
- MongoDB 交接 BIZ：[BIZ-20260824-01](BIZ-20260824-01-mongodb-fact-binding-handoff.md)
- Schema 2.0 草稿业务审核：[BIZ-20260826-01](BIZ-20260826-01-schema2-draft-business-review.md)
- DeepSeek 调用治理：[BIZ-20260827-01](BIZ-20260827-01-deepseek-call-governance.md)
- 规则 3.0 与 Agent 2 交接决策：[BIZ-20260902-01](BIZ-20260902-01-rule-v3-agent2-ready-boundary.md)
- 当前遗留缺陷：[BUG-20260819-01](BUG-20260819-01-rule-ast-semantic-loss.md)（等待修订草稿业务复审）、[BUG-20260826-01](BUG-20260826-01-schema2-business-review-blockers.md)（已在未持久化草稿中技术修订，等待复审）、[BUG-20260902-01](BUG-20260902-01-latest-rule-agent1-semantic-rejection.md)（最新两条规则的历史真实 Agent 1 候选均被语义门禁拒绝）、[BUG-20260906-05](BUG-20260906-05-agent1-v2-real-call-convergence-blocker.md)（2026-09-06 真实 Agent 1 V2 生成落库 5 次运行 21 次调用未收敛，等待用户决策）
- 上次实施记录：[PROG-20260819](PROG-20260819.md)
- 上次新会话交接：[PROG-20260824](PROG-20260824.md)
- 当前审核记录：[PROG-20260826](PROG-20260826.md)
- 当前实施记录：[PROG-20260827](PROG-20260827.md)
- 当前资料整合记录：[PROG-20260901](PROG-20260901.md)
- 当前 Agent 1 与改进方案记录：[PROG-20260902](PROG-20260902.md)
- 当前收尾与纠偏记录：[PROG-20260903](PROG-20260903.md)
- V3 收尾缺陷：[BUG-20260903-01](BUG-20260903-01-v3-confirmation-and-validation-gaps.md)
- 真实 V3 单次调用缺陷：[BUG-20260903-02](BUG-20260903-02-agent1-v3-single-call-schema-rejection.md)
- V3 正式落库与当日文档纠偏记录：[PROG-20260907](PROG-20260907.md)

## 当前状态

Phase 0 文档基线、Phase 1.0 后端骨架和 Phase 1.1-1.5 规则解析闭环均已完成。项目运行时锁定为 Python `3.11.9`，依赖管理采用 `venv + pip + pyproject.toml`，Agent 编排框架为 LangGraph，模型供应商为 DeepSeek。

后端现已具备 FastAPI/Uvicorn 服务、集中配置、脱敏配置检查、PyMongo Async 生命周期、MongoDB Schema v5 幂等迁移、健康检查和自动化测试。Rule Parsing Agent 已实现 camelCase JSON 契约、确定性语义校验、四视图字段目录、LangGraph 有限重试图、DeepSeek JSON Output 适配器、受控 Markdown 读取、HTTP 文本入口和 CLI 文件入口；规则版本仓储支持显式不可变保存与精确版本回读，事实交接仓储和 V3 恢复仓储支持 insert-only 幂等保存和哈希冲突保护，V3 交付持久化（`rule_versions_v3` 与单文档 `fact_binding_handoff_batches_v3`）已按 [REQ-20260906-01](REQ-20260906-01-v3-mongodb-persistence.md) 实现，并于 2026-09-07 按用户授权完成本机正式库落库与回读复核（见 [PROG-20260907](PROG-20260907.md)）。

Phase 1.6 已按用户授权完成：增加显式、不可变的 MongoDB 草稿版本归档，并保存一版《项目报告释放规则》。规则仍固定为待人工审核的 `draft` 且不可执行。

2026-08-19 完成了已归档 JSON 向 Agent 2 交接的只读兼容性评审。评审确认 JSON 的版本、来源、Schema 和候选映射结构便于程序读取，但归档条件树存在公式和分支语义丢失，且现有校验未能阻止；已记录为 [BUG-20260819-01](BUG-20260819-01-rule-ast-semantic-loss.md)。该草稿不得作为 Agent 2、规则执行或发布输入。

用户确认双服务事实级交接基线后，Phase 1.7 已完成：应用版本升级为 `0.4.0`，新解析固定生成 Schema `2.0.0`；结构化表达式、事实种类/粒度/参数/派生、严格引用闭包、类型校验和确定性案例执行已落地；历史 Schema `1.0.0` 保持只读兼容。新增按精确规则版本导出非派生 `FactBindingRequest[]` 的 HTTP 入口，RuleReader 内仍不实现 SQL、目标库元数据或 Agent 2 模型调用。

上述实现已通过提交 `44731b6` 推送到 RuleReader `origin/main`。独立 SqlBot 截至 2026-08-24 已推进到提交 `37a659a`，完成事实绑定输入之后的确定性规则变更分析；其 SQL 生成仍未实现。跨仓库后续工作必须分别遵守各自 REQ/BIZ/DEV，不能在 RuleReader 内补写 SqlBot 能力。

Phase 1.8 按用户要求补齐事实交接契约：应用版本升级为 `0.5.0`；新增 `FactBindingRequest 2.0.0` 的逻辑实体、字段、筛选、聚合、时间范围、标量结果、完整来源证据和结构化不确定性。当前规则草稿无法确定的实体键、物理字段、筛选完整性、聚合函数或时间语义会显式导出为 `unresolved + blocking`，不会被猜测。该阶段原有旧版默认策略已被 Phase 1.9 的用户明确决定替代。

Phase 1.9 当前应用版本为 `0.7.0`：事实绑定 HTTP 默认与显式调用只接受 `2.0.0`，`1.0.0` 和未知版本均返回 422；历史 V1 类型只供离线兼容测试。两批真实 DeepSeek 候选均被门禁拒绝后，用户明确授权受控 `reviewed_import`，且不再调用 Provider。该路径以独立来源标识复用同一可信校验，完成真实 Schema `2.0.0` 草稿的专项审计、不可变保存、精确回读和全部事实请求导出；33 个请求均有阻断不确定性，因此没有启动 SQL 阶段。

Phase 1.10 当前应用版本为 `0.8.0`、MongoDB Schema 为 v3：新增 RuleReader 唯一写入、SqlBot 只读的 `fact_binding_handoffs`。指定已持久化 V2 规则已确定性写入 33 条完整 `FactBindingRequest 2.0.0`；重复执行插入 0 条，33 条 canonical payload 哈希、Pydantic/静态 JSON Schema、安全门禁、请求身份和 blocking uncertainties 均通过回读复核。V1 历史规则仍为 0 条交接，两个 `rule_versions` 原始 BSON 哈希在前后保持一致。

2026-08-26 已对精确版本 `REPORT_RELEASE_ALL_001@20260824T080726492666Z-562eabd40e5a` 完成只读业务审核。65 个条件节点的核心 AST 与来源一致，历史版本的公式和产品 `759` 互斥分支丢失未复现；但 41 个事实中仍有粒度/参数边界未冻结，4 个费用候选映射指向计算口径不同的辅助视图字段，`report.merge_flag` 值域与案例冲突，20 个案例还有 15 个单侧覆盖节点。审核结论为未批准，详见 [BIZ-20260826-01](BIZ-20260826-01-schema2-draft-business-review.md) 和 [BUG-20260826-01](BUG-20260826-01-schema2-business-review-blockers.md)。本次没有修改规则、交接记录、应用版本或 MongoDB Schema。

2026-08-27 按用户明确授权完成 [REQ-20260827-01](REQ-20260827-01-schema2-business-review-remediation.md)：应用升级到 `0.9.0`，MongoDB Schema 保持 v3；新的 `reviewed-import-v2` profile 修正事实粒度/参数、复合原始数据条件、案例输入门禁和候选映射，并增加双向覆盖案例。真实来源离线生成 `REPORT_RELEASE_ALL_001@20260827T013225952760Z-562eabd40e5a`，包含 42 个事实、67 个条件、31 个案例和 2 个候选映射；34 条非派生事实请求全部通过契约与安全门禁且保持 blocking。该版本只存在于 Git 忽略的本地产物目录，未写入 MongoDB，也未获得业务批准。

2026-08-27 用户随后独立选择任务 B，并完成 [REQ-20260827-02](REQ-20260827-02-deepseek-retry-audit-idempotency.md)：应用升级到 `0.10.0`，MongoDB Schema 保持 v3；DeepSeek 可重试调用增加有界指数退避，成功与最终错误携带 request ID、逐次 outcome、Provider completion ID 和可用 token usage。HTTP/CLI 支持只保存 SHA-256 的进程内幂等键；同键同解析身份合并并重放成功结果，同键异身份在调用 Provider 前失败。该任务没有继续 Schema 2.0 业务复审，也没有调用真实 Provider、连接 MongoDB 或修改规则/交接记录。

2026-09-01 SqlBot 的独立任务 C 及后续离线 V2 安全链已在其仓库完成。项目外 17 个字段映射、视图
定义、规则原文和设计文件另存到私有 `colglaze/RuleDataReferences` 的固定 commit
`2240e5bd18e36d17650896a10cc61e1c18e3daa0`；bundle digest 为
`6d403f1a110ea1699a72aec76f38be944583670f96767f5e3f44b2ac2e565160`。RuleReader 公开仓库只登记这
两个身份，不复制内部字段清单或完整 SQL，也不增加运行时 reader、数据库访问或授权能力。

2026-09-02 按用户明确要求，从权威附件第 1.3 节分别提取项目报告与原始数据现行规则，并以对应
视图作为查询实现证据调用真实 Agent 1。两条规则各完成 3 次 `deepseek-v4-flash` 调用，最终均为
`CANDIDATE_SEMANTIC_INVALID`，没有生成规则 JSON、没有连接 MongoDB，也没有绕过门禁继续重试。
历史失败摘要见 [BUG-20260902-01](BUG-20260902-01-latest-rule-agent1-semantic-rejection.md)。

用户随后要求编写改进方案。已形成 [REQ-20260902-01](REQ-20260902-01-rule-contract-v3-agent2-ready-handoff.md)、
[BIZ-20260902-01](BIZ-20260902-01-rule-v3-agent2-ready-boundary.md) 和
[DEV-20260902-01](DEV-20260902-01-rule-contract-v3-agent2-ready-handoff.md) 草案，提出多阶段/优先级/多结果
Rule Schema 3.0、业务确认事实目录、分阶段 Agent 1 和零阻断 Agent 2 模板请求。当前仅完成文档，
未修改代码、未再次调用 Provider，也未授权 SqlBot、SQL 模板、持久化、发布或执行。
随后新增 [DEV-20260902-02](DEV-20260902-02-agent1-v3-prompt.md)，冻结可复用的 `ruleStructure`
单阶段 System Prompt 与 User Payload；文档中提及的 `ruleStructure/testCases` 两阶段调用模板只有
设计意图，testCases 阶段未实现、未经真实 Provider 验证。该 Prompt 当前仅批准用于离线
`ruleStructure`，未替换生产 Prompt。

用户随后批准 REQ/BIZ/DEV 进入 Slice 1/2。已离线实现 `RuleStructureCandidateV3`、确认事实目录、
固定规则块提取、引用闭包/类型/优先级/digest 门禁和多 outcome 解释器；生产 Prompt 与运行时默认
仍保持 V2。2026-09-03 最终复核纠正了首轮目录重写确认值、制造 binding profile 和遗漏来源冲突的问题。
当时摘要记录固定私有 bundle 在内存中生成 7 个证据充分的事实、19 个源规则节点，其中 14 个明确阻断。
XLSX 确认值不被改写；缺失的真实 binding profile 保持为空。详情以
[PROG-20260903](PROG-20260903.md) 为准，首轮“38 个事实、两个阻断”的结果已撤销。

2026-09-05 完成 V3 MongoDB 恢复（Schema v4、`rule_structure_candidates_v3` 1 条）后，用户随后下达
纯离线 Agent 2 readiness 任务：确定性比较拒绝恢复旧 V2（路径 B），冻结独立
`FactBindingRequest 3.0.0`；16 项 blocking 全部保留，readiness=false，计划规则版本数与 handoff 数
均为 0。工程随后完成全量离线验证：默认套件 `116 passed, 3 deselected`，Ruff、严格 Mypy（55 个
源文件）与契约导出幂等复核通过，4 份脱敏产物确定性再生且经扫描确认不含私有正文、物理字段、SQL、
凭据或本机路径。详见 [PROG-20260905](PROG-20260905.md)。

## Phase 1.0 完成证据

- Python `3.11.9` 环境成功安装固定依赖；Ruff 和 Mypy 均通过。
- 默认测试结果为 `8 passed, 1 deselected`；MongoDB integration 测试结果为 `1 passed, 8 deselected`。
- 用户的 `mongodb` 容器已启用认证。凭据仅通过临时进程环境变量用于验收，未写入仓库或日志。
- `rule_reader` 数据库已连续初始化两次，均为 Schema v1；只创建 `schema_migrations` 和 `app_metadata` 基础设施集合。
- 真实服务成功启动，根路径、存活、就绪和脱敏配置端点均返回 200；就绪端点报告 Schema v1。
- 详细命令与结果见 [PROG-20260818](PROG-20260818.md)。

## Phase 1.1-1.5 完成证据

- 默认离线测试为 `24 passed, 2 deselected`；Ruff 通过，Mypy 通过并检查 27 个源码文件。
- MongoDB integration 为 `1 passed, 25 deselected`；真实 DeepSeek provider integration 为 `1 passed, 25 deselected`。
- 《项目报告释放规则》通过真实 DeepSeek 解析：27 个必需事实、11 个已映射字段、16 个待确认字段、13 个测试案例。
- 《原始数据释放规则》通过真实 DeepSeek 解析：22 个必需事实、7 个已映射字段、15 个待确认字段、11 个测试案例。
- 两份输出均为 Schema `1.0.0`、`status=draft`、`executable=false`，版本号包含规则 ID、UTC 微秒时间戳和来源哈希前 12 位。
- 真实服务在 `127.0.0.1:8766` 启动并完成 MongoDB Schema v1 检查；`POST /api/v1/rules/parse` 经 DeepSeek 返回 200，随后服务正常关闭。
- 详细验证、问题修复和安全边界见 [PROG-20260818](PROG-20260818.md)。

## 当前前置依赖

- MongoDB 与 DeepSeek 配置均已通过脱敏配置检查；Phase 1.9 不再受 Provider 非法候选阻塞。后续阻断来自事实请求中尚未解决的实体键、字段、筛选、聚合或时间范围，而不是连接或凭据。
- 使用 CLI 时，待解析 `.md` 文件必须放在 `RULEREADER_DOCUMENT_ROOT` 内；HTTP 入口可直接提交文本。
- 两份真实文档的字段映射均为候选结果；用户本次授权先归档一版草稿，不代表候选映射已通过业务审核。
- [BUG-20260819-01](BUG-20260819-01-rule-ast-semantic-loss.md) 对应来源已在私有
  `RuleDataReferences` bundle 中按仓库相对路径保存，规范化 SHA-256 仍为
  `562eabd40e5a5701fb9515499b542a6e2ff46056464621b1abb0a7c37f116e4d`。
- 私有 bundle 中的 Wiki 现行规则是另一份内容，规范化哈希为
  `ec70dd785f619b814a0d4f74497f9026bc1ce47aedcc62efae44c8a6cb8c32b7`；未经用户确认不得替代上述
  缺陷来源。

## Phase 1.6 完成证据

- 应用版本升级为 `0.3.0`，正式数据库从 Schema v1 幂等升级到 v2；`schema_migrations` 中 v2 记录恰好一条。
- `rule_versions` 已建立唯一版本索引、规则编号/生成时间联合索引和来源哈希索引。
- CLI 使用 `--persist` 实际保存 `REPORT_RELEASE_ALL_001@20260818T092511165521Z-562eabd40e5a`。
- 正式库 `rule_versions` 总记录数为 1，目标版本记录数为 1。
- HTTP 精确回读返回 200，内外版本号一致；记录入库时间为 `2026-08-18T09:25:11.166000+00:00`。
- 入库草稿为 Schema `1.0.0`、`draft`、不可执行，包含 39 个事实、39 个映射候选（10 个 `mapped`、29 个 `unresolved`）和 13 个测试案例。
- 默认测试为 `27 passed, 2 deselected`，MongoDB integration 为 `1 passed, 28 deselected`；Ruff、Mypy 和依赖完整性检查通过。

## Phase 1.7 完成证据

- 应用版本升级为 `0.4.0`，Parser Prompt 版本升级为 `rule-parser-v2`；新解析只输出 Schema `2.0.0`。
- Schema `2.0.0` 使用结构化左右表达式，支持算术、`coalesce`、`dateAdd` 和 `source/aggregate/exists/derived` 事实。
- 确定性校验覆盖引用闭包、未知事实、未使用事实、派生环、类型/操作符、映射完整性和测试案例实际执行结果。
- MongoDB 仓储同时回读 Schema `1.0.0` 与 `2.0.0`；旧版事实交接返回 `409 RULE_SCHEMA_UNSUPPORTED_FOR_BINDING`。
- `GET /api/v1/rules/versions/{ruleVersion}/fact-binding-requests` 为每个非派生事实导出一个 camelCase 请求。
- 默认离线测试：`36 passed, 2 deselected`；MongoDB integration：`1 passed, 37 deselected`；Ruff、严格 Mypy 和 `pip check` 通过。
- 跨仓库互操作验证使用 RuleReader 实际导出的 `task.settlement_fee` 请求，SqlBot 原样读取后返回 `ready` 且无阻塞问题。
- 没有调用真实 DeepSeek、没有改写正式 MongoDB 数据，也没有实现 SQL 或目标库元数据能力。

## Phase 1.8 完成证据

- [FactBindingRequest 2.0.0 Schema](../contracts/fact-binding-request-2.0.0.schema.json) 使用 Draft 2020-12、稳定 `$id`、camelCase、必填版本常量和封闭对象；Schema 由 Pydantic 权威模型确定性生成。
- `queryRequirements` 强制包含 `entity/fields/filters/aggregation/timeRange/result`；聚合与时间范围使用受控结构，SQL 字符串、凭据、物理 JOIN 和目录 `sourceExpression` 不进入交接。
- 来源追溯包含源名称、相对路径（可空）、完整 SHA-256、字符数、生成时间、Parser/Prompt/Provider/模型版本和指向规则草稿的 JSON Pointer 证据。
- 合法未决、非法缺少 `timeRange` 和旧 `1.0.0` 三类静态样例已加入；旧版可由冻结模型解析但被新版 Schema 拒绝。
- 原端点默认仍返回 `FactBindingRequest 1.0.0`；`?contractVersion=2.0.0` 返回新版，未知版本返回 422，Schema `1.0.0` 规则仍返回原有 409。
- 只读跨仓库复核确认当前 SqlBot 能解析冻结的旧版样例，并按其现有严格模型拒绝带新增字段的 `2.0.0`；这证明过渡默认值仍有必要，且新版 intake 必须在 SqlBot 独立实现。
- 相关契约/API 回归为 `25 passed`；默认离线测试为 `44 passed, 2 deselected`；MongoDB integration 为 `1 passed, 45 deselected`；Ruff、严格 Mypy（35 个源码文件）和 `pip check` 通过。
- 未调用真实 DeepSeek；integration 只使用随机测试库并在结束时删除；没有新增或调用 Agent 2、Text-to-SQL、SQL 执行或目标数据库访问。

## Phase 1.9 当前证据

- 应用版本升级为 `0.6.0`，公共 `FactBindingRequest` 别名和 HTTP 响应模型只指向 V2；默认与显式 `2.0.0` 返回新版，显式 `1.0.0` 和未知版本返回 422。
- Parser Prompt 升级为 `rule-parser-v6`，明确公式 AST、互斥分支、事实参数、点分 `factCode`、允许的表达式/条件节点、案例覆盖、不确定性和无 SQL/凭据要求。
- LangGraph 校验重试最多反馈 100 条字段路径并携带 `previousCandidate`；修正版仍重新执行 Pydantic、语义闭包和确定性解释器，代码不补字段。
- 首轮同一来源真实重解析被 `CANDIDATE_SCHEMA_INVALID` 拒绝；失败项包括非数组 `allowedValues`、无 `derivation` 的派生事实和契约外条件节点。没有保存失败候选。
- 用户随后授权“最多三次”；硬上限包装器确认恰好调用 3 次。追加轮最终仍为 `CANDIDATE_SCHEMA_INVALID`：`requiredFacts` 中的大量 `factCode` 及其在 `derivation`、`rootCondition`、`fieldMappings` 中的引用不符合 `^[a-z][a-z0-9_]*(?:\.[a-z][a-z0-9_]*)+$`。失败发生在专项审计与持久化之前。
- 用户随后明确同意不调用 DeepSeek 的受控 `reviewed_import`。Parser 追溯为 `reviewed_import / reviewed-import-v1 / codex-gpt-5`，来源仍为 7,501 字符和完整哈希 `562eabd4...`。
- 专项 profile 包含 41 个事实（33 个非派生、8 个派生）、65 个唯一条件和 20 个案例；解释器与结构审计覆盖完整费用 AST、759 两档、显式 `NOT` 未结束流程、普通/直接路径、空值、互斥分支及 60/75/180 天精确边界。
- 新不可变草稿 `REPORT_RELEASE_ALL_001@20260824T080726492666Z-562eabd40e5a` 已保存并精确回读。正式库现有 2 条：历史 Schema `1.0.0` 1 条、新 Schema `2.0.0` 1 条。
- 回读结果逐一导出 33 条 `FactBindingRequest 2.0.0`；全部通过 Pydantic 与 Draft 2020-12 JSON Schema，且全部含 `blocking` 不确定性。规则与请求均未携带目录表达式、可执行 SQL、连接串或凭据。
- 正式 FastAPI 只读验收返回：精确版本 200、默认事实请求 200 且共 33 条 V2、显式 V1 请求 422。
- 最终离线测试为 `52 passed, 2 deselected`，MongoDB 隔离集成为 `1 passed, 53 deselected`；Ruff、严格 Mypy（37 个源码文件）和 `pip check` 通过。完整记录见 [PROG-20260824](PROG-20260824.md)。

## Phase 1.10 完成证据

- MongoDB v3 migration 创建 `fact_binding_handoffs`；索引为内建 `_id_`、唯一 `uq_fact_binding_handoffs_request_id`、唯一 `uq_fact_binding_handoffs_rule_version_fact_code`。
- 正式命令 `rule-reader persist-handoffs --rule-version REPORT_RELEASE_ALL_001@20260824T080726492666Z-562eabd40e5a` 首次返回 `records=33, inserted=33, existing=0`；第二次返回 `records=33, inserted=0, existing=33`。
- 正式库 33 条记录拥有 33 个唯一 request ID 和 fact code，契约版本仅 `2.0.0`；Pydantic、仓库内 Draft 2020-12 Schema、安全文本、canonical SHA-256 和身份复核均为 `33/33`，全部请求仍含 blocking uncertainty。
- `rule_versions` 正式库前后均为 2 条。历史 V1 BSON SHA-256 保持 `546dab2823449e626f546024452f20b9b078804e90110afdaff09e5d6fb89a81`，目标 V2 保持 `e13eea6befc0591fd29a478a96cfb17ced3f9e5b7447caf1032e1da9bf18dd36`；V1 交接记录为 0。
- 默认离线测试为 `57 passed, 3 deselected`；MongoDB integration 为 `2 passed`；Ruff、严格 Mypy（42 个源码文件）和 `pip check` 通过。
- 本阶段没有调用 DeepSeek、读取源 Markdown、访问 SQL Server、生成 SQL、保存连接串/凭据或修改任何 `rule_versions` 记录。

## Post-Phase 1.10 业务审核阻断修订证据

- 新草稿版本：`REPORT_RELEASE_ALL_001@20260827T013225952760Z-562eabd40e5a`，Schema `2.0.0`、`draft`、不可执行、`reviewed_import / reviewed-import-v2 / codex-gpt-5`。
- 结构为 42 个事实（34 个非派生）、67 个条件、31 个案例和 2 个保留候选映射；67/67 条件均有 `pass/fail` 双向覆盖。
- 34 条本地导出的 `FactBindingRequest 2.0.0` 全部通过 Pydantic、静态 JSON Schema 和安全门禁，并全部保持 blocking；没有调用持久化用例。
- 新规则/请求 JSON SHA-256 分别为 `5d67f0d26aea117ac16528095553c524a7f97bb74fdfb9a02b8cb519cd7dc108` 和 `2e97b9ab725cc5c9268f24d58559aea7638274994efdad2425f332db6070bb53`。
- 旧规则/请求导出 SHA-256 仍为 `993a527f94fb38281d9804cee3f507c9b25cdcaf7600e1af50df805f3ff57fdd` 和 `7e8d52f0373500c69d8cc17302568cdaabf83a31244738f4fddb9d3cccee0f91`。
- 默认离线测试 `65 passed, 3 deselected`；Ruff、严格 Mypy（42 个源码文件）和 `pip check` 通过。没有运行 MongoDB 或 Provider integration。

## Post-Phase 1.10 DeepSeek 调用治理证据

- 应用版本为 `0.10.0`；Python `3.11.9`、MongoDB Schema v3 和规则 Schema `2.0.0` 不变，没有 migration 或新集合。
- 第一次调用无等待；后续 retryable 尝试按基础值指数增长并在最大值封顶，最后或非 retryable 失败后不再等待。
- `parser.audit` 和最终 `error.audit` 提供 request ID、尝试总数、逐次退避/outcome/retryable、Provider completion ID、逐次及汇总 token usage；历史 reviewed import 不新增 `audit: null`。
- 进程内幂等覆盖同键顺序重放、并发在途合并、同键异身份 409、无效键 422、失败后重新尝试和有界 LRU 淘汰；原始键不进入结果或错误。
- 默认离线测试为 `83 passed, 3 deselected`；Ruff、严格 Mypy（43 个源码文件）和 `pip check` 通过。没有运行 MongoDB 或真实 Provider integration。

## Rule Schema 3.0 Slice 1/2 离线收尾证据

- 静态 Schema：
  `contracts/rule-structure-candidate-3.0.0.schema.json`、
  `contracts/business-confirmed-fact-catalog-3.0.0.schema.json`；
- 权威项目报告规则块：1,402 字符，SHA-256
  `f285643e5b2bb2ec7b13861716407afda4252c2fbc81eb75a4b0bb3ba4b37c6d`；
- 目录仅保留 7 个证据充分事实，binding profile 缺失不伪造；最终身份与 digest 见
  [PROG-20260903](PROG-20260903.md)；
- 历史规则结构摘要：5 个阶段、19 个规则节点、6 个 active 闭包事实、14 个 blocked 节点；
- V3 专项 `59 passed`；完整默认离线测试 `142 passed, 3 deselected`；
- Ruff、严格 Mypy（49 个源码文件）和依赖检查通过；
- 真实资料复核只读取固定 commit 的 XLSX/7 个视图/规则文档，没有执行 SQL；
- 没有调用 Provider、MongoDB、SqlBot 或数据库，没有生成测试案例、版本、发布或执行状态，也没有
  持久化候选。
- 2026-09-05 换机恢复后，用户确认 XLSX 已填写数据有效并指定第 1.3 节有序规则为 V3 权威来源；
  source-bound 恢复结果为 7 个事实、5 个阶段、19 个规则节点（3 active、16 blocked），catalog digest
  为 `f2bf1c99d27c379559fff1e0311ffa8a202759ce466be6262a5611dc28c8518d`。恢复文件写入固定私有仓库
  之外的同级恢复目录，仍为不可执行、未持久化候选；固定 bundle 保持干净并通过原 digest 校验。

## V3 Agent 2 交接就绪门禁完成证据

- 路径门禁：`readiness_v3.py` 五项 V2/V3 等价性门禁（source identity、stage order、priority、
  multi-outcome、postGate/exclusion）全部 fail，确定固定路径 B，禁止恢复 V2 remediation
  （`562eabd4...` ≠ 有序 V3 `f285643e...`）。
- 16 项 blocking 分类：`BUSINESS_FACT_MISSING` 12、`SOURCE_VALUE_CONFLICT` 3、
  `SOURCE_BRANCH_UNREACHABLE_WITH_CONFIRMED_ENUM` 1；覆盖 stateGuards/prerequisites/eligibility/
  postGates/exclusions，owner 均为 `businessRuleReview`，无一项可由代码安全自动修复。
- 16 条 readiness gate：12 fail、2 blocked（导出被 blocking 阻止）、2 pass（provenance 闭包、
  不可执行状态）；`ready=false`。
- 契约：`bindings_v3.py` 冻结独立 `FactBindingRequest 3.0.0`（V3 ruleRef、stage/rule/priority/
  condition/outcome usage、evidence 闭包、仅 warning uncertainty、类型层拒绝 derived fact），静态
  Schema 为 [contracts/fact-binding-request-3.0.0.schema.json](../contracts/fact-binding-request-3.0.0.schema.json)，
  合法/非法样例齐备；blocking 载荷被拒绝。SqlBot 当前只支持 `2.0.0`，需在其仓库独立升级。
- 落库计划：仅计划未来 Schema v5 的 `rule_versions_v3` 与单文档原子
  `fact_binding_handoff_batches_v3`（同 ID 同 hash 幂等、异 hash 冲突、写入前全批验证、写入后全批
  回读、`rule_structure_candidates_v3` 只读）；本任务未实现或执行任何 migration。
- 离线产物（`RuleDataReferences-recovery/agent2-readiness/`，再生前后 SHA-256 一致）：
  `v2-v3-compatibility.json` `a75e36ade00eaab3339983439afd2bf59b081e36004235a30bc0efdfe45ea950`、
  `v3-agent2-readiness.json` `7134dca1af084aee99cb371338f718f632903cf06dcc42041be6f8cb2cb6af81`、
  `business-confirmation-checklist.md` `fe8238c9b83c08c0a1a5119d2cc6c3ceebbd5c2f10b6321af7f4f7f74646dc38`、
  `mongodb-persistence-plan.json` `8ee3a6ba288ffe6393ad42da59544de7949da2dfe66c33cd49eb8d7a5c12a9ec`。
- 产物脱敏扫描：无私有规则正文/枚举值、物理字段、SQL、凭据或本机路径；中文仅为生成器硬编码文案。
- 最终验证：`python -m pytest -q` 为 `116 passed, 3 deselected`；Ruff check/format、严格 Mypy
  （55 个源文件）、契约导出幂等、`git diff --check` 与 `.obsidian/workspace.json` 无差异均通过。
- 本任务没有访问或修改 MongoDB、没有调用 DeepSeek、没有连接 SQL Server、没有生成/执行 SQL、
  没有修改 SqlBot、没有提交或推送；`RuleParseResultV3=0`、`handoff=0`。

## V3 候选业务确认与解锁完成证据（2026-09-06）

- 用户以《优化方案》全文质疑信息充分性后，逐类完成 16 项 blocking 裁决并记录于
  [BIZ-20260906-01](BIZ-20260906-01-v3-blocker-business-confirmation.md)：12 项缺失事实按方案
  V2.0 语义确认；`task.status_code` 补录 19（XLSX G8 描述自证"主释放任务要求 19"）；两个标志位
  以 XLSX 确认值域 {4,5} 为准（L 列原文"4是5否"）；报告有无空值=待定保留分支。
  [BUG-20260905-02](BUG-20260905-02-v3-agent2-readiness-blockers.md) 已关闭。
- 新增 `scripts/report_release_v3_confirmed_profile.py`：构建 19 个确认事实目录与 19 节点全
  active 候选，构建时执行候选校验、19 条可达性见证与方案 SHA-256 核验，全部 fail-fast。
- `readiness_v3.py` 16 条门禁改为按状态确定性推导；被阻断状态证据文案逐字节不变，历史
  analyze 产物重跑 SHA-256 完全一致。
- 解释器语义落定：终态空值放行（`nullPolicy=fail`）、资格首个命中保留 READY、postGates 以
  "未满足即命中"降级（`merge_group_eligible ne true`）、exclusions 按 outcome 白名单产出等待、
  requiredFactCodes 等于条件引用闭包（18 个）；值域最小化使事实缺失时确定性 INDETERMINATE。
- 确认产物（`RuleDataReferences-recovery/report-release-v3-confirmed/`）：catalog digest
  `82dbd05a800a5df27291423f348ab451436f1271eb485ae2c495e40abf6328da`，candidate 19 active /
  0 blocking / executable=false；脱敏扫描命中均为既有已接受产物同级别内容（XLSX 确认单元格
  原文、公开脚本既有视图溯源名），无私有正文整句、SQL、凭据或本机路径；固定 bundle 复验通过。
- 最终验证：`121 passed, 3 deselected`（新增 5 个确认 profile 测试）；Ruff check/format、
  严格 Mypy（55 个源文件）、契约导出、`git diff --check` 与 `.obsidian/workspace.json` 均通过。
- 本任务没有访问或修改 MongoDB、没有调用 DeepSeek、没有连接 SQL Server、没有生成/执行 SQL、
  没有修改 SqlBot、没有提交或推送；`RuleParseResultV3=0`、`handoff=0`。

## Slice 3-6 离线完成证据（2026-09-06，用户授权后）

- 实现 [DEV-20260906-01](DEV-20260906-01-rule-parse-result-v3.md)：`result_v3.py` 提供
  `RuleParseResultV3`（不可变 ruleVersion、source/parser/catalog/candidate 闭包、
  factDeclarations=BindableFactV3+QueryRequirementsV3、testCases）与
  `FactBindingRequest 3.0.0` 导出器（拒绝 blocking/非 ready、每非派生事实一请求、
  requestId=`<ruleVersion>#<factCode>`、mapping 保持 unresolved、warning-only uncertainties）；
  `readiness_v3.py` 增加可选 result/requests 全量推导，None 路径逐字节不变。
- 生成不可变版本 `REPORT_RELEASE_ALL_001@20260905T172407000000Z-f285643e5b2b-82dbd05a800a`：
  18 条事实声明（16 source、1 aggregate、1 exists、0 derived）、20 个测试案例（全部 7 个
  outcome、资格首个命中、postGate 降级、exclusion、INDETERMINATE 不补造）、导出 18 条请求；
  **16/16 门禁全 pass，ready=true**。
- 产物（`report-release-v3-confirmed/`，catalog/candidate 与此前一致，固定 generatedAt 幂等）：
  result `16fafc47...`、requests `dfca3a6d...`、readiness `67e5ae23...`、manifest
  `0ef3af69...`（完整 SHA-256 见 [PROG-20260906](PROG-20260906.md)）。
- 最终验证：`122 passed, 3 deselected`；Ruff、严格 Mypy（56 个源文件）、契约导出、
  `git diff --check`、`.obsidian/workspace.json` 均通过；历史 analyze 产物字节级不变。
- 本任务没有访问或修改 MongoDB、没有调用 DeepSeek、没有连接 SQL Server、没有生成/执行 SQL、
  没有修改 SqlBot、没有提交或推送；所有 V3 交付物保持 `executable=false`、待审核、
  mapping unresolved。

## 换机环境与文档一致性修复（2026-09-06）

- Python `3.11.9` 已安装在 `D:\Python\python.exe`；裸命令只命中 Windows Store alias，原 `.venv`
  又保留旧用户目录解释器路径。已使用实际解释器原位重建 `.venv` 并安装锁定依赖。
- 使用固定输入与固定 `generatedAt` 重新生成 confirmed 产物；catalog、candidate、result、readiness
  哈希与既有记录一致，requests 与 manifest 的可复现 SHA-256 已更正为 `dfca3a6d...`、`0ef3af69...`。
- README、PRD、技术设计、实施文档以及相关 REQ/DEV/BUG 已同步到 Slice 3-6 离线完成状态；业务
  确认前的 blocked 结论保留为历史阶段记录。
- 当时完整默认离线测试 `128 passed, 3 deselected`（当前基线 `203 passed, 6 deselected`，
  2026-09-07 复核）；Ruff check/format、严格 Mypy（56 个源文件）
  和 `pip check` 通过。当时机器未发现 Git，故未执行 `git status`/`git diff --check`（第三轮
  复核更正：Git 已安装于 `D:\LeStoreDownload\Git\cmd\git.exe`，仓库触发 dubious ownership，
  需单次 `git -c safe.directory=D:/Python/pyWorkspace/RuleAgent ...` 执行只读检查；以上 Git
  位置与 safe.directory 指引均为 2026-09-06 旧机器历史记录，不代表当前环境）；没有提交或推送。
- 详细复现、原因与证据见
  [BUG-20260906-01](BUG-20260906-01-reinstall-environment-and-v3-document-drift.md)。

## V3 交付 MongoDB Schema v5 持久化完成证据（2026-09-06，用户授权后）

- 用户明确授权完成“RuleReader MongoDB Schema v5 持久化”的代码、自动化测试和文档同步，并允许
  应用版本升级到 `0.12.0`；授权明确排除真实 MongoDB 写入、执行持久化命令、调用 DeepSeek、
  连接 SQL Server、生成/执行 SQL、修改 SqlBot、提交/推送。范围、数据结构、失败语义与幂等规则
  冻结于 [REQ-20260906-01](REQ-20260906-01-v3-mongodb-persistence.md)、
  [BIZ-20260906-02](BIZ-20260906-02-v3-mongodb-persistence.md) 和
  [DEV-20260906-02](DEV-20260906-02-v3-mongodb-persistence.md)；AGENTS.md 的 migration 允许
  集合清单已加入 `rule_versions_v3` 与 `fact_binding_handoff_batches_v3`。
- 先修复 [BUG-20260906-02](BUG-20260906-02-confirmed-profile-version-coupling.md)：
  `scripts/report_release_v3_confirmed_profile.py` 的 parserVersion 不再读取
  `rule_reader.core.version.__version__`，冻结为常量 `CONFIRMED_PROFILE_PARSER_VERSION = "0.11.0"`；
  新增 2 个回归测试证明 parser provenance 与应用包版本解耦。应用升级到 0.12.0 后以固定输入与
  固定 `generatedAt=2026-09-05T17:24:07+00:00` 向临时目录再生 confirmed 产物，catalog、candidate、
  result、requests、readiness、manifest 六个文件 SHA-256 与既有产物逐字节一致（临时目录已删除，
  私有 bundle 复验 digest `6d403f1a...` 不变，未覆盖任何既有产物）。
- Schema v5 migration：`LATEST_SCHEMA_VERSION = 5`，新建且仅新建两个集合并建立三个唯一索引
  （`uq_rule_versions_v3_rule_version`、`uq_rule_versions_v3_source_catalog`、
  `uq_fact_binding_handoff_batches_v3_rule_version`）；幂等、支持并发初始化，不修改 v1-v4
  集合与历史记录。第一轮曾让普通启动无条件升到 v5，该授权缺口已在第二轮
  [BUG-20260906-03](BUG-20260906-03-v3-persistence-authorization-and-integrity-gaps.md) 修复为
  显式 target_version 分级（见下文验收修订证据）。
- 应用层新增 `src/rule_reader/application/v3_persistence/`（canonical/ports/service）与
  `src/rule_reader/infrastructure/v3_persistence.py`：insert-only、同 hash 幂等保留首次时间、
  异 hash 稳定脱敏冲突；写前一次性完成 blocking、readiness 16/16、source/catalog/candidate
  hash 闭包、请求身份/数量门禁；写入后精确回读并复核 `rule_versions`、`fact_binding_handoffs`、
  `rule_structure_candidates_v3` 计数不变。batch 为单文档、按 requestId 确定性排序、时间不参与
  hash；未引入事务、消息队列、第三个集合或 API endpoint。
- 新增显式脚本 `scripts/persist_report_release_v3_delivery.py`（`--artifact-dir`）：先完成产物
  解析、可信 manifest 自身 SHA-256 加 manifest 内记录的五个交付文件 SHA-256、readiness 16/16 与
  写前闭包校验，全部通过后才初始化 MongoDB；
  输出仅含状态、schema version、ruleVersion、哈希、计数、inserted/existing 的脱敏摘要。本任务
  只实现和测试该脚本，未实际运行连接数据库。
- 测试：新增 `test_migrations_v5.py`（6 个）、`test_v3_persistence.py`（14 个）、
  `test_persist_report_release_v3_delivery.py`（4 个）与合成 ready 交付夹具
  `tests/v3_delivery_fixtures.py`；版本绑定测试期望（`test_app.py`、`test_rule_workflow.py`）
  同步到 0.12.0。全部默认离线测试 **154 passed, 3 deselected**（基线 128 passed + 本次 26 个）；
  `ruff check .`、`ruff format --check .`（175 个文件）、严格 Mypy（61 个源文件）、`pip check`
  均通过。
- integration 测试未运行：未获真实数据库授权；`tests/integration` 未在本次修改（Schema v5
  integration 覆盖需在授权后另行补充运行）。
- 本任务没有访问或修改 MongoDB、没有调用 DeepSeek、没有连接 SQL Server、没有生成/执行 SQL、
  没有修改 SqlBot、没有提交或推送；`RuleParseResultV3` 正式库数量仍为 0、V3 handoff 仍为 0。

## Schema v5 持久化验收修订完成证据（2026-09-06 第二轮，[BUG-20260906-03](BUG-20260906-03-v3-persistence-authorization-and-integrity-gaps.md)）

- 文档先行：新增 [BUG-20260906-03](BUG-20260906-03-v3-persistence-authorization-and-integrity-gaps.md)
  （8 项已确认复现缺口），并在
  [REQ-20260906-01](REQ-20260906-01-v3-mongodb-persistence.md)、
  [BIZ-20260906-02](BIZ-20260906-02-v3-mongodb-persistence.md)、
  [DEV-20260906-02](DEV-20260906-02-v3-mongodb-persistence.md) 追加“验收修订/安全加固”章节，
  历史正文未改写。随后先写失败测试并确认红态（迁移分级收集失败、4 个回读缺口与 13 个脚本
  缺口在修复前失败），再实现修复。
- 迁移授权分级：`RUNTIME_SCHEMA_VERSION=4`、`V3_PERSISTENCE_SCHEMA_VERSION=5`、
  `LATEST_SCHEMA_VERSION=5`；`apply_migrations` 接收显式 `target_version`，普通 serve、
  `rule-reader init-db`、V1/V2 持久化、V2 handoff、V3 恢复默认只到 v4 且不创建 V3 持久化集合；
  仅 V3 脚本校验通过后请求 v5；v5 库上普通应用返回实际版本 5，不降级、不重写；非法/越界
  target 在任何写入前抛错；无新增 Settings 开关。
- 唯一获批产物绑定：脚本先校验 manifest 文件自身 SHA-256 等于冻结常量
  `0ef3af69...`（信任锚），再校验 manifest 记录的五个交付文件哈希，并强制固定 ruleVersion
  `REPORT_RELEASE_ALL_001@20260905T172407000000Z-f285643e5b2b-82dbd05a800a`、requestCount=18、
  testCaseCount=20、ready=true、blocking=0、executable=false；synthetic/自签名/再生 manifest
  一律拒绝，无 `--allow-any`/`--skip-hash`/环境变量绕过；离线测试以 monkeypatch 模块常量构造
  synthetic 成功路径。
- 回读加固：存量 wrapper 顺序必须严格升序且与顶层 `request_ids` 逐项一致；request ID 与
  factCode 不得重复；wrapper `created_at` 为 aware UTC 且等于 batch `created_at`；rule
  `stored_at` 为 aware UTC，naive 时间一律拒绝（不再静默补 UTC）；三层 contractVersion 一致。
- CLI 错误统一脱敏：畸形 request → `V3PersistenceContractError`；`MongoStartupError` →
  `V3PersistenceUnavailableError`；`main` 向 stderr 输出 `{code,message,retryable,details:[]}`
  并以退出码 1 结束，无 traceback、无 payload/`input_value`/URI/绝对路径。摘要状态新增
  `recoveredAndVerified`（规则已存在、batch 本次补写），任一新插入不得报告
  `existingAndVerified`。
- integration 测试更新（未运行）：`test_mongodb.py` 常规目标显式 v4，新增显式 v5 目标迁移
  （两新集合、三唯一索引、v1-v4 不变、v5 库普通启动）与 V3 首写/重放/异内容冲突代码路径；
  `test_fact_binding_handoffs_mongodb.py` 常规目标显式 v4。
- 环境与文档：`.venv/pyvenv.cfg` 的 `command` 修正为 `D:\Python\Python311\python.exe`；
  进度文档删除“`.obsidian/workspace.json` 当前有修改”的失实陈述（该文件本身与已提交状态
  无差异）；manifest 校验描述统一为“可信 manifest 自身 SHA-256 + manifest 内记录的五个交付
  文件 SHA-256”；README/PRD/技术设计/实施文档统一说明运行时默认 v4、仅脚本请求 v5、本轮未
  访问 MongoDB（最新文档证据为 2026-09-05 的 Schema v4 与一条 V3 recovery 记录）且未授权写入。
- 本轮验证：默认测试 **173 passed, 5 deselected**（含新增 2 个 integration 测试的反选）；
  `ruff check .`、`ruff format --check .`、严格 Mypy、`pip check` 与 Markdown 相对链接检查
  通过（实际命令见 [PROG-20260906](PROG-20260906.md) 第二轮记录）。integration 测试未运行
  （未授权真实数据库）；未运行持久化脚本、`rule-reader init-db`、`rule-reader serve`；Git
  位于 `D:\LeStoreDownload\Git\cmd\git.exe`，dubious ownership 保护下以单次
  `git -c safe.directory=D:/Python/pyWorkspace/RuleAgent ...` 执行只读检查；本轮未访问
  MongoDB、未写入；未提交或推送。

### 第三轮复核修订（2026-09-06，[BUG-20260906-03](BUG-20260906-03-v3-persistence-authorization-and-integrity-gaps.md) 复核发现）

- 复核确认 8 项问题并重开缺陷：Settings 构造在错误转换之外、integration 乱序 batch 幂等
  语义错误、基线数字（514 个链接）、Git 真实位置与 dubious ownership、工作区存在大量未提交/
  未跟踪 V3 文件、`.obsidian/workspace.json` 单独无差异、MongoDB 历史访问史更正。
- 修复：`Settings()` 构造的 Pydantic `ValidationError` 转换为
  `V3PersistenceUnavailableError("V3 persistence configuration is invalid")`，Settings 失败时
  不构造 `MongoManager`、不连数据库；integration 语义更正为“应用服务输入顺序无关（乱序
  requests 经 `prepare_v3_delivery` 排序后幂等重放）+ 仓储拒绝乱序非法 prepared record
  （期待 `V3PersistenceConflictError`）”，并新增服务级顺序无关 integration 测试代码；
  [DEV-20260906-02](DEV-20260906-02-v3-mongodb-persistence.md) R3/R6 补充对应语义。
- 文档证据更正：删除“本机没有可用 Git”与“真实 MongoDB 从未被访问”的陈述；统一为 Git 位于
  `D:\LeStoreDownload\Git\cmd\git.exe`（单次 `-c safe.directory` 只读检查、`git diff --check`
  通过、工作区大量未提交/未跟踪、仅 `.obsidian/workspace.json` 无差异），以及本轮未访问
  MongoDB、历史证据为 2026-09-05 Schema v4 + 一条 V3 recovery 记录、实时状态未重新确认。

## integration 隔离安全门禁完成证据（2026-09-06 第四轮，[BUG-20260906-04](BUG-20260906-04-isolated-mongodb-integration-safety.md)）

- 缺口：integration 测试在 `RULEREADER_TEST_MONGODB_URI` 缺失时回退 `Settings().mongodb_uri`
  （可能误连环境数据库）；`database_created` 在首次 migration 成功后才置 true（migration
  中途失败不清理随机测试库）。
- 修复：新增纯测试侧门禁 `tests/integration/mongodb_test_guard.py`（不进入 `src`、不成为
  生产依赖、导入不连库）：只读 `RULEREADER_TEST_MONGODB_URI` 与
  `RULEREADER_TEST_MONGODB_ALLOW_WRITE` 两个环境变量；URI 缺失明确失败、禁止回退 Settings
  或 `.env`；写入确认值必须精确为 `isolated-local-only`（错误消息不回显实际值）；拒绝
  `mongodb+srv`（scheme 先拒，避免 DNS）；PyMongo `parse_uri` 解析后所有节点必须是
  `localhost`/`127.0.0.1`/`::1`；拒绝远程 IP/域名、空 host、无法解析的 URI；错误消息不含
  完整 URI/用户名/密码/环境变量值；无命令行/Settings/默认值绕过。
- 两个 integration 测试统一改用门禁取得 URI；`ping` 成功后、任何写入之前即武装
  `cleanup_database` 标志，migration/索引/insert 中途失败也会在 `finally` 删除随机测试库；
  删除前再次断言库名以 `rule_reader_test_` 开头；不枚举、不批量删除、不用用户配置库名。
- 顺带修复 `test_persist_report_release_v3_delivery.py` 中 `capsys.readouterr()` 二次读取的
  无效脱敏断言，改为对首次捕获的 stderr JSON 断言。
- 验证：门禁离线测试 14 passed；脚本专项 14 passed；三个 Schema v5 文件 44 passed；默认
  套件 **189 passed, 6 deselected**（5 个 integration 用例仅导入/收集）；Ruff check/format、
  严格 Mypy（61 源文件）、`pip check` 通过；`git diff --check` 通过、`.obsidian/workspace.json`
  无差异、工作区 97 行状态不 clean；链接检查通过（数量见 PROG）。integration 未运行：
  两个测试环境变量当前均未配置；真实 MongoDB 未访问、未写入。
- 下一任务排序更新：隔离 integration 通过前，不得把“真实 Schema v5 落库”列为立即下一步。

## integration 拓扑门禁收紧证据（2026-09-06 第五轮，[BUG-20260906-04](BUG-20260906-04-isolated-mongodb-integration-safety.md) 拓扑复核）

- 复核发现：仅 nodelist 为 loopback 不足以证明连接停留在本机——门禁此前仍接受
  `?replicaSet=prod`、`?directConnection=false`、`?loadBalanced=true` 等可能触发驱动拓扑
  发现或多节点路由的选项。
- 修复：URI query option 采用严格 allowlist（仅 `directConnection`、`authSource`、
  `authMechanism`，键名小写比较）；`directConnection` 必须显式 `true`；`replicaSet` 与
  `loadBalanced` 无论取值均拒绝；allowlist 外已知 option 拒绝并只回显 option 名（不回显
  值）；pymongo 未知 option 由 parse 层拒绝并经脱敏边界转换；错误消息不回显完整 URI、凭据、
  数据库名或 option 实际值；不修改 `os.environ`、不连网、不读 Settings/.env、无新增依赖。
- 失败测试先行：7 个拒绝类测试收紧前失败；收紧后门禁离线测试 **25 passed**（覆盖缺失
  directConnection、false、replicaSet、loadBalanced、allowlist 外选项、未知 option、三类
  回环主机 + directConnection=true、含认证/authSource/directConnection=true 的回环 URI、
  脱敏断言）。
- 5 个 integration 用例清理逻辑复核继续满足（随机库名、ping 后武装清理、finally 断言前缀、
  精确 drop、中途失败也清理、不打印 URI/凭据）。
- 验证：默认套件 **201 passed, 6 deselected**；Ruff check/format（179 files already
  formatted）、严格 Mypy（61 源文件）、`pip check`、`git diff --check`（`.obsidian/
  workspace.json` 无差异）通过；Markdown 相对链接 **527 个全部有效**。
- **integration 未运行**：只读存在性检查确认两个环境变量均未配置（未输出任何值），门禁
  前置条件不满足，不连接、不 ping；当前执行状态 `AWAITING_ISOLATED_MONGODB_CONFIGURATION`；
  真实 MongoDB 未访问、未写入。
- **第八轮执行预检（2026-09-06）**：用户授权“若环境变量已配置则运行隔离 integration”后，
  只读存在性检查确认两个变量**均仍未配置**（未输出任何值），按授权边界未连接、未 ping、
  未运行 integration，未读取或推断 `.env`/`RULEREADER_MONGODB_URI`，未修改门禁；离线基线
  复核 guard 25 passed、三个相关文件 44 passed、`git diff --check` 通过、工作区 99 行不
  clean；状态保持 `AWAITING_ISOLATED_MONGODB_CONFIGURATION`；真实 MongoDB 未访问、未写入。

## 真实 Agent 1 V2 生成落库尝试（2026-09-06，[BUG-20260906-05](BUG-20260906-05-agent1-v2-real-call-convergence-blocker.md) 未收敛）

- 用户要求"调用 Agent 1 生成一版 JSON 规则并写入 MongoDB"（不使用 reviewed_import 直插）。
  链路：`rule-reader parse --file 项目报告释放规则.md --persist`；来源经规范化核验为 7,501
  字符、SHA-256 `562eabd4…` 的权威原文。
- 环境事实：MongoDB 容器当日约 19:32 重建，`rule_reader` 库为空，历史 2 条规则、33 条交接、
  1 条 V3 recovery 不在当前实例；DeepSeek 凭据运行时取自同级 SqlBot 本地未跟踪 `.env` 注入
  进程环境（未打印、未落盘）；`Settings` 的 base/model 默认为 None，需显式环境变量。
- 5 次运行、21 次真实 `deepseek-v4-flash` 调用（约 107.7 万 tokens）全部被门禁拒绝，失败面
  逐级前移：输出截断（maxTokens 16384）→ Schema（`right.value` null 字面量）→ 语义闭包/日期
  类型 → 仅剩 6 个测试案例 indeterminate。Prompt 经 `rule-parser-v7`（判空一元操作符）、
  `rule-parser-v8`（引用闭包、`runtime.current_date` 与 `dateAdd` 同型比较、案例 given 完整性）
  两轮修订；校验器新增 indeterminate 案例的缺失事实指针（先红后绿的 2 个回归测试）。
- 第 4、5 次运行中模型自第 2 次尝试起输出逐字节相同（含明确缺失清单反馈仍不修改 testCases），
  判定为模型行为局限；未写入任何 MongoDB 规则（`rule_versions` 为 0，仅空库上初始化的
  Schema v4 基础设施集合）。
- 离线全绿：**203 passed, 6 deselected**、Ruff check/format（179 文件）、严格 Mypy（61 文件）。
  5 份脱敏运行日志当时存 `artifacts/v2-agent1-real-runs-20260906/`（2026-09-07 复核：该目录在
  当前工作区不存在，原始日志构成证据缺口，见 BUG-20260906-05"证据状态"）；完整审计、根因与
  三个关闭路径见 [BUG-20260906-05](BUG-20260906-05-agent1-v2-real-call-convergence-blocker.md) 与
  [PROG-20260906](PROG-20260906.md)。未提交、未推送。

## V3 Schema v5 正式落库完成证据（2026-09-07，用户授权后）

- 落库前只读检查确认：本机实例为原始正式库（Schema v3、`rule_versions` 2 条、
  `fact_binding_handoffs` 33 条，service 0.8.0 时代），历史数据从未丢失；
  `mongodb-snapshot-20260906` 描述的空库是换机重建后的另一实例。
- `persist_report_release_v3_delivery --artifact-dir generated-rules/report-release-v3-confirmed`
  → `persistedAndVerified`：信任锚与写前门禁通过后升级 Schema v3→v5，落库
  `REPORT_RELEASE_ALL_001@20260905T172407000000Z-f285643e5b2b-82dbd05a800a`
  （rule payload `fb6c1e3c…`、batch `0df35b35…`、18 条 `FactBindingRequest 3.0.0`）；
  历史集合计数 2/33/0 前后不变。
- `persist_report_release_v3_recovery --reference-root ../RuleDataReferences`
  → `persistedAndVerified`：恢复候选 `REPORT_RELEASE_ALL_001@f285643e5b2b-f2bf1c99d27c`
  （catalog digest `f2bf1c99…` 与仓库归档一致，7 事实、19 节点、16 blocked、
  `executable=false`）写入 `rule_structure_candidates_v3`。
- 第二次交付脚本重放为 `existingAndVerified`（零重复插入）；独立复核：migrations v1-v5
  齐全、`app_metadata` 升至 0.12.0/v5、batch 18 个 wrapper 的 request ID/factCode 唯一、
  顶层 `request_ids` 顺序逐项一致、`contract_version` 全为 3.0.0、全部 payload 哈希
  独立重算匹配。
- 全程未调用 DeepSeek、未连接 SQL Server、未修改 SqlBot、未提交推送；integration 测试
  仍未运行（两个测试环境变量未配置），本次落库不等价于 integration 验收。
  详细证据见 [PROG-20260907](PROG-20260907.md)。

## 下一任务

**历史记录（2026-09-02 至 2026-09-05 的 DeepSeek 调用阶段，仅供追溯）**：用户随后单独允许最小
V3 接入及真实 DeepSeek 一次调用，并在首次 5 处条件 id 格式错误后，另行授权强化 ID Prompt 再调用
一次。两次独立任务均各完成 1 次请求；当时结果仍为 CANDIDATE_SCHEMA_INVALID（4 处 factCode 格式
错误），未进入语义校验。用户随后单独批准改进 Prompt 与有界纠错（最多新增 3 次请求），真实任务
只调用 1 次便触发业务确认停止条件；模型声明 20 个待核对事实，并有目录外条件引用和 ruleCode
全局重复，仍无有效 candidate。边界以
[BIZ-20260902-01](BIZ-20260902-01-rule-v3-agent2-ready-boundary.md) 为准；历史摘要见
[PROG-20260903](PROG-20260903.md) 和
[BUG-20260903-02](BUG-20260903-02-agent1-v3-single-call-schema-rejection.md)。该阶段没有产出
有效模型规则，也不影响后述 confirmed V3 交付；ruleStructure 最小分支与有界纠错已在各自独立授权
下完成，不会自动追加模型请求或消费历史剩余次数。

**当前真正未完成的下一任务**（按依赖顺序）：

1. **真实 Agent 1 V2 生成落库的路径决策（当前下一任务，[BUG-20260906-05](BUG-20260906-05-agent1-v2-real-call-convergence-blocker.md)）**：
   用户须在三个关闭路径中选择——①授权继续有界重试（模型已连续 10 次输出相同候选，预期收益低）；
   ②按新 REQ/DEV 把 V2 解析改为“结构/案例两阶段”调用（参考 DEV-20260902-02 的 ruleStructure
   单阶段模板；两阶段方案待设计、待真实 Provider 验证，不保证收敛或消除截断）；
   ③该文档沿用 reviewed_import 路线，回到隔离 integration 序列（V3 Schema v5 持久化已于
   2026-09-07 完成）。决策前不得再自动发起任何真实 Provider 调用。
2. **在显式配置的一次性 loopback MongoDB 上运行 integration（状态
   `AWAITING_ISOLATED_MONGODB_CONFIGURATION`）**：用户需提供或设置 loopback-only 测试 MongoDB
   URI（`RULEREADER_TEST_MONGODB_URI`，仅 `mongodb://`、主机为 localhost/127.0.0.1/::1、必须
   包含 `directConnection=true`、query option 仅允许 directConnection/authSource/
   authMechanism），并显式设置写入确认值
   `RULEREADER_TEST_MONGODB_ALLOW_WRITE=isolated-local-only`；随后仅对随机
   `rule_reader_test_<uuid>` 数据库运行 integration（门禁会拒绝一切其他配置）。拓扑门禁已在
   第五轮收紧完成；当前两个环境变量仍未配置，因此 integration 仍未运行。integration 通过
   前，不得把“真实 Schema v5 落库”列为立即下一步。
3. **~~真实执行 V3 Schema v5 持久化~~（已完成，2026-09-07）**：用户已授权并执行
   `persist_report_release_v3_delivery`（Schema v5，`persistedAndVerified`，18 条请求）与
   `persist_report_release_v3_recovery`（恢复候选 1 条）；幂等重放与独立复核通过，本机原始
   正式库历史数据原样保留，证据见 [PROG-20260907](PROG-20260907.md)。
4. **SqlBot 独立升级 `FactBindingRequest 3.0.0` intake**（SqlBot 仓库）：当前 SqlBot 只支持
   `2.0.0`；V3 交接可消费性取决于其仓库独立 REQ/BIZ/DEV 与升级，RuleReader 不提供静默兼容或
   降级。两个标志位 0/1 与 4/5 的编码差异必须在 SqlBot 物理绑定阶段复核；既有逻辑确认不因模型
   建议而撤销，binding profile 缺失也不自动补造。
5. **旧 V2 修订草稿的业务复审（需用户单独授权）**：按精确新版本
   `REPORT_RELEASE_ALL_001@20260827T013225952760Z-562eabd40e5a` 复审 42 个事实、67 个条件、
   31 个案例和 2 个候选映射；复审只读私有 bundle/本地产物，不得把草稿标记为已发布或可执行，
   持久化仍需在复审通过后另行授权。SqlBot Phase 5 也必须在其仓库独立建立 REQ/BIZ/DEV 后才能
   启动。

## 遗留问题

- [BUG-20260906-05](BUG-20260906-05-agent1-v2-real-call-convergence-blocker.md)：真实 Agent 1
  V2 生成《项目报告释放规则》5 次运行 21 次调用未收敛；失败面已收窄到测试案例一致性，且模型
  在明确缺失清单反馈下连续输出相同候选。关闭路径需用户决策（见"下一任务"第 1 项）。
- 2026-09-06 晚间 MongoDB 容器重建后 `rule_reader` 库为空：历史 2 条 `rule_versions`、33 条
  `fact_binding_handoffs`、1 条 V3 recovery 记录仅存在于文档与历史证据中，不在当前实例；
  依赖这些记录的回读/对比操作（如 V2 handoff、精确版本回读验收）在重新落库前不可执行。
  2026-09-07 更正与补充：上述"容器重建"针对换机后的另一实例；本机原始正式实例历史数据
  完整保留（2 条规则、33 条交接），并已于 2026-09-07 按用户授权完成 V3 confirmed 交付
  （Schema v5）与 V3 恢复候选落库，见 [PROG-20260907](PROG-20260907.md)。
- Wiki 接入时间、Provider 和同步策略未确定，且不属于当前阶段。
- SqlBot 的 V2 intake、元数据授权解析、独立候选输入和 AST 静态门禁已完成；Phase 5 受限 SQL Server
  验证仍未规划，不得由 RuleReader 实现或授权。
- 已归档 Schema `1.0.0`《项目报告释放规则》仍保留 AST 语义缺陷并禁止交接；已持久化 Schema `2.0.0` 版本的业务审核未通过；新的未持久化修订草稿等待复审，详见 [BUG-20260819-01](BUG-20260819-01-rule-ast-semantic-loss.md)。
- 四视图目录只确认 SQL 文本中的显式输出；`v_ReportDataReleaseRules` 的 `t1.*` 未擅自展开，未精确命中的事实保持 `unresolved`。
- DeepSeek 重试、尝试审计和客户端幂等键已完成应用层治理；幂等缓存仍只限单一服务进程，不跨 worker/重启共享，也没有独立审计检索或费用账单对账能力。
- 2026-09-02 项目报告与原始数据规则的历史真实 Agent 1 调用均在语义门禁后被拒绝：这些历史
  DeepSeek 调用没有产出有效 candidate，因此没有可交付的新模型规则；但 reviewed_import 的
  confirmed V3 交付（`RuleParseResultV3` 与 18 条请求）已可交付并可经 Schema v5 持久化，
  摘要见 [BUG-20260902-01](BUG-20260902-01-latest-rule-agent1-semantic-rejection.md)。
- 当前规则 Schema `2.0.0` 尚未声明事实参数角色、完整事实检索筛选、聚合函数和时间范围；新版交接通过阻塞不确定性暴露该缺口。不得在导出器中从描述文本推断这些值。
- 已持久化 V2 草稿仍保留事实粒度、参数、值域、案例覆盖和 6 个金额/费用候选问题；现有不可变版本不修改。上述问题只在新的未持久化草稿中修订，且仍需业务复审，详见 [BUG-20260826-01](BUG-20260826-01-schema2-business-review-blockers.md)。
- SqlBot 已独立完成 `FactBindingRequest 2.0.0` 只读 intake；任一 blocking 项仍确定性阻止候选生成，
  RuleReader 不再提供旧版降级。
- 早期记录中的“当前 Git 工作区包含用户已有的 `.obsidian/workspace.json` 修改”已失效：
  2026-09-06 复核确认 `.obsidian/workspace.json` 本身与已提交状态无差异，本仓库各任务也未修改
  该文件。当时记录的“工作区大量未提交/未跟踪 V3 工作文件、不得描述为 clean”与“Git 位于
  `D:\LeStoreDownload\Git\cmd\git.exe`、仓库触发 dubious ownership、需以单次
  `git -c safe.directory=D:/Python/pyWorkspace/RuleAgent ...` 执行只读检查”均为 2026-09-06
  旧机器历史记录，不代表当前环境；当前状态见下条。
- 换机后以用户确认 XLSX 和有序规则重建的 V3 `REPORT_RELEASE` 候选的 16 项来源冲突/事实缺口
  已按 [BIZ-20260906-01](BIZ-20260906-01-v3-blocker-business-confirmation.md) 全部裁决关闭；
  工程解锁不等于业务批准发布或可执行，candidate 仍为不可执行待审核产物。
- 独立 `FactBindingRequest 3.0.0` 已随路径 B 冻结，但 SqlBot 仍只支持 `2.0.0`；V3 交接可消费性
  取决于 SqlBot 在其仓库独立建立 REQ/BIZ/DEV 后升级 intake，RuleReader 不提供静默兼容或降级。
- 线下释放/批量释放标志的现行视图 0/1 比较编码与 XLSX 确认值域 {4,5}（4=是、5=否）存在编码
  差异，已按裁决以 XLSX 为准；物理绑定复核前不得据此生成可执行导出。
- Git 状态以仓库实际引用为准：2026-09-07 实测（纠偏任务开始时）本地 `main`、`origin/main` 与
  实时远端（`git ls-remote`）一致，均指向 `01ddae0`（`docs: record 20260906 delivery commit
  bad6fd3`），工作区 clean；本轮文档纠偏与 [PROG-20260907](PROG-20260907.md) 的改动按约定保持
  未提交。2026-09-06 经用户明确授权，累计未提交的 V3/Schema v5/integration
  门禁/Agent 1 v8/generated-rules 工作已以提交 `bad6fd3`（117 个文件）推送至
  `origin/main`（`65a9684..bad6fd3`）；早期记录中的 `d420a33` 不是当前仓库的 main 指向，不得再
  断言其为最新交付。
