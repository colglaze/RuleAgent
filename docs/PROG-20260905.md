# PROG-20260905：修复不完整 V3 提交的离线阻塞

- 关联缺陷：[BUG-20260905-01](BUG-20260905-01-incomplete-v3-commit.md)
- 状态：`COMPLETED_OFFLINE_AGENT2_READINESS_BLOCKED_BY_BUSINESS`
- 检查对象：`65a96846b3ae991b42a8159dbbee43a9bbe15846`。

## 已完成

确认 V3 导入、Schema 导出和文档引用没有对应交付文件；缺失清单和处理边界统一记录在 BUG 中。
先解除 V2 收集阻塞，再依据可达设计重建 V3 确认目录、规则结构、验证/解释器、规则块提取、Prompt、
LangGraph 有界纠错、显式 DeepSeek 入口、脱敏夹具、静态 contracts 和 20260902–03 文档。

只读检查 SqlBot `3d23162` 的契约与治理文档，确认 SqlBot 仍是 V2 独立 consumer，私有 bundle 固定为
`2240e5bd...` 且 `authority=none/executable=false`。没有修改 SqlBot 工作区。

用户提供的外部 `REPORT_RELEASE_ALL_001` 成功按唯一 ID 提取，身份为 SHA-256
`f9d7187d5c1b545cdcc1fbf075dcd126bc26f91a5b6737d16d5e6adb5d98e307`、6,136 字符。正文、内部字段和
视图口径没有复制进公开仓库；规则本身不能替代确认事实目录。

初始 `git status --porcelain=v1 --untracked-files=all` 为空；没有覆盖用户修改，
没有修改 `.obsidian/workspace.json`、提交、推送、连接 MongoDB 或调用真实 DeepSeek。

## 验证

本地建立被 Git 忽略的 `.venv`，运行时为 Python 3.11.9；依赖严格取自 `requirements-dev.lock`，
项目以 `--no-deps -e .` 安装，未修改版本或锁文件。环境准备允许下载依赖，测试本身使用默认离线套件。

- 修复前：`python -m pytest -q`，16 个收集错误。
- 新增回归修复前：`python -m pytest -q tests/unit/test_repository_artifacts.py`，1 failed。
- 首轮解除阻塞后完整默认套件：84 passed、3 deselected。
- V3 就近测试：目录/候选、静态 Schema、解释器、来源、工作流、真实 profile 和契约重建共
  22 passed。
- `python -m scripts.export_contract_schemas` 成功并生成 2 个 V3 Schema、4 个 V3 样例；
  重复导出一致性由自动化测试覆盖。
- 最终完整默认离线套件：`python -m pytest -q`，110 passed、3 deselected。
- `python -m ruff check .` 与 `python -m ruff format --check .` 通过。
- `python -m mypy` 通过，53 个源码文件无问题。
- `uv pip check --python .venv/Scripts/python.exe` 通过，64 个包兼容。
- `git diff --check` 通过；`.obsidian/workspace.json` 无差异。

以上 Python 命令均通过 `.venv/Scripts/python.exe` 执行。未运行真实 Provider 或 MongoDB integration。

## 边界与下一任务

公开 V3 源码、夹具、contracts 与 20260902–03 文档已重建。历史原始模型响应、私有业务资料及原文件
字节没有恢复，也不需要进入公开仓库。

用户随后确认私有 XLSX 中已填写数据均已核对有效，“待补充”状态只是未更新；并指定私有优化方案
第 1.3 节有序项目报告规则为 V3 权威结构来源。私有仓库固定 commit 已检出并通过自带校验；有序规则
块精确恢复为 1,402 字符及历史 SHA-256 `f285643e...`。当前继续构建 source-bound V3 catalog/candidate；
XLSX 与规则的编码冲突保持 blocking，binding profile 不从参考资料推断，也不调用真实 Provider。

恢复 profile 已完成并写入固定私有仓库之外的同级目录
`RuleDataReferences-recovery/report-release-v3/`，避免改变 bundle 的受管文件集合和 digest：

- catalog：7 个用户确认事实，digest
  `f2bf1c99d27c379559fff1e0311ffa8a202759ce466be6262a5611dc28c8518d`，文件 SHA-256
  `d43077aa37d0d113f4373e69949d7207e963755d6cc5e25e4c106605758e60d0`；
- candidate：5 个阶段、19 个规则节点、3 active、16 blocked，文件 SHA-256
  `47f363ce44d02ce4d2bb8026243d7752008b38053073c8057166c3c0d1aaa53f`；
- active 节点仅使用确认目录内且与有序来源无冲突的事实；缺失业务事实、编码冲突、合并组与 OA 范围
  继续 blocking；
- 结果为 `validatedBlockedCandidate / executable=false`，没有调用 DeepSeek、MongoDB 或 SQL。
- 固定私有仓库在产物移出后重新通过自带 `verify_bundle.py`，工作区干净，仍为 17 个源文件、2 个派生
  文件和原 content digest `6d403f1a...`。

用户随后明确要求直接恢复到当前电脑 MongoDB。已完成 [REQ-20260905-01](REQ-20260905-01-v3-mongodb-recovery.md)：

- 应用升级到 `0.11.0`，当前 `rule_reader` 从旧 Schema 幂等升级到 v4；
- 新集合 `rule_structure_candidates_v3` 首次保存返回 `inserted=true`，立即重放返回
  `inserted=false`；
- candidate ID：`REPORT_RELEASE_ALL_001@f285643e5b2b-f2bf1c99d27c`；
- canonical payload SHA-256：
  `368e1389c525d2103d2d7039b6a159e0f2254eaf4f49534be50b7048e149eef0`；
- 精确回读仍为 7 个确认事实、19 个规则节点、16 个 blocking、`executable=false`；
- 当前新电脑 `rule_versions=0`、`fact_binding_handoffs=0`、V3 恢复记录 `=1`，证明本次没有把 V3
  混入旧集合；历史 V1/V2 数据仍需单独数据库备份或重建。
- 认证 MongoDB 隔离 integration 为 `2 passed`；连接凭据只从容器运行环境注入单个进程，没有写入
  `.env`、源码、日志或输出。

## V3 Agent 2 交接就绪门禁

用户随后提供独立任务，明确本任务只允许离线代码、契约、测试、文档和待审核产物，不允许写 MongoDB、
调用 DeepSeek、访问 SQL Server、生成 SQL 或修改 SqlBot。本节开始后未再访问或修改 MongoDB。

确定性比较拒绝路径 A：V2 remediation 来源为 `562eabd4...`，有序 V3 来源为 `f285643e...`；V2
缺少五阶段、首个命中优先级、多 outcome、postGate 和 exclusion 载体。选择路径 B，并冻结独立
`FactBindingRequest 3.0.0`，当前 SqlBot 需要后续契约升级。

16 项 blocking 分类为：12 个 `BUSINESS_FACT_MISSING`、3 个 `SOURCE_VALUE_CONFLICT`、1 个
`SOURCE_BRANCH_UNREACHABLE_WITH_CONFIRMED_ENUM`。16 项均阻止交接且无确定性自动修复。readiness
16 个门禁中仅 provenance 闭包和不可执行状态通过；当前 RuleParseResultV3 数 0、handoff 数 0。

离线产物位于 `RuleDataReferences-recovery/agent2-readiness/`：

- `v2-v3-compatibility.json`：SHA-256 `a75e36ade00eaab3339983439afd2bf59b081e36004235a30bc0efdfe45ea950`；
- `v3-agent2-readiness.json`：SHA-256 `7134dca1af084aee99cb371338f718f632903cf06dcc42041be6f8cb2cb6af81`；
- `business-confirmation-checklist.md`：SHA-256 `fe8238c9b83c08c0a1a5119d2cc6c3ceebbd5c2f10b6321af7f4f7f74646dc38`；
- `mongodb-persistence-plan.json`：SHA-256 `8ee3a6ba288ffe6393ad42da59544de7949da2dfe66c33cd49eb8d7a5c12a9ec`。

产物不包含私有规则正文、物理字段、SQL、凭据或本机路径。计划采用未来 Schema v5 的独立
`rule_versions_v3` 与单文档原子 `fact_binding_handoff_batches_v3`；本任务没有实现或执行 migration。

## 会话交接记录（用量中断前）

### 1. 用户目标与最新授权

当前目标是解决 MongoDB 没有 SqlBot 可消费规则和事实交接的问题。最新任务只授权读取、修改本地
代码/契约/测试/文档、生成离线不可执行产物和运行默认离线测试；明确禁止写 MongoDB、调用真实
DeepSeek、连接 SQL Server、生成/执行 SQL、修改 SqlBot、提交或推送。

注意：在这份最新任务开始之前，用户曾明确要求把 V3 恢复到当前 MongoDB。当时已完成 Schema v4 和
一条 `rule_structure_candidates_v3` 写入；最新任务开始后没有再次访问或修改 MongoDB。后续会话必须
继续遵守最新的“只离线、不写库”限制。

### 2. 已恢复的来源与用户确认

- 私有仓库本地路径：`D:\Python\pyWorkspace\RuleDataReferences`；固定 commit
  `2240e5bd18e36d17650896a10cc61e1c18e3daa0`。
- 私有 bundle 自带校验通过：17 个源文件、2 个派生产物、digest
  `6d403f1a110ea1699a72aec76f38be944583670f96767f5e3f44b2ac2e565160`。
- 用户确认字段清单 XLSX 中已填写的数据全部经过核对且有效；“待补充”只是状态列未更新。
- 用户指定《项目报告和原始数据释放优化方案》第 1.3 节的有序项目报告规则作为 V3 权威结构来源。
- 有序规则块为 1,402 字符，SHA-256
  `f285643e5b2bb2ec7b13861716407afda4252c2fbc81eb75a4b0bb3ba4b37c6d`。
- 规则顺序为 `R0 > R1 > R9 > R4 > R2 > R3 > R8 > R5 > R6 > R7`。
- V2 remediation 使用另一份 7,501 字符来源，SHA-256
  `562eabd40e5a5701fb9515499b542a6e2ff46056464621b1abb0a7c37f116e4d`。

### 3. 当前 V3 恢复结果

离线恢复文件位于：

```text
D:\Python\pyWorkspace\RuleDataReferences-recovery\report-release-v3\
```

- catalog：7 个确认事实，digest
  `f2bf1c99d27c379559fff1e0311ffa8a202759ce466be6262a5611dc28c8518d`；
- candidate：5 个阶段、19 个规则节点、3 active、16 blocked；
- 状态：`validatedBlockedCandidate / executable=false`；
- candidate ID：`REPORT_RELEASE_ALL_001@f285643e5b2b-f2bf1c99d27c`；
- Mongo payload SHA-256：
  `368e1389c525d2103d2d7039b6a159e0f2254eaf4f49534be50b7048e149eef0`。

最新任务开始前的当前 MongoDB 状态为 Schema v4、V3 恢复记录 1 条、`rule_versions=0`、
`fact_binding_handoffs=0`。不得在最新任务中改变这些计数。

### 4. V2/V3 决策门禁

已确定选择路径 B，禁止路径 A。原因：

- V2/V3 来源 SHA-256 不同；
- V2 只有单一 root condition 和 pass/fail；
- V2 无法原生保存五阶段顺序、首个命中 priority、多 outcome、postGates 或 exclusions；
- `FactBindingRequest 2.0.0.ruleRef.schemaVersion` 固定为 `2.0.0`，不能精确引用 V3。

因此不得运行旧 V2 remediation exporter 生成当前 SqlBot 输入，也不得把 V3 candidate 写入 V2
`rule_versions` 或直接转换为 V2 handoff。

### 5. 16 项 blocking 分类

- `BUSINESS_FACT_MISSING`：12；
- `SOURCE_VALUE_CONFLICT`：3；
- `SOURCE_BRANCH_UNREACHABLE_WITH_CONFIRMED_ENUM`：1。

它们覆盖 stateGuards、prerequisites、eligibility、postGates、exclusions，owner 均为
`businessRuleReview`，全部阻止 Agent 2 交接，当前没有可由代码安全自动修复的项目。

脱敏报告位于：

```text
D:\Python\pyWorkspace\RuleDataReferences-recovery\agent2-readiness\
```

- `v2-v3-compatibility.json`
- `v3-agent2-readiness.json`
- `business-confirmation-checklist.md`
- `mongodb-persistence-plan.json`

报告已扫描确认不包含私有规则正文、物理字段、SQL、凭据或本机路径。当前 readiness 为 false，计划
RuleParseResultV3 数 0、handoff 数 0。

### 6. 本会话新增代码与契约

已完成：

- `readiness_v3.py`：V2/V3 等价性门禁、blocking 分类、16 条 readiness gate；
- `analyze_v3_agent2_readiness.py`：生成上述四份脱敏离线产物；
- `bindings_v3.py`：独立 `FactBindingRequest 3.0.0` 契约，包含 V3 ruleRef、完整
  entity/fields/filters/aggregation/time/result、stage/rule/priority/condition/outcome usage、provenance、
  evidence 闭包及 warning-only uncertainty；derived fact 被类型层拒绝；
- `contracts/fact-binding-request-3.0.0.schema.json`；
- V3 binding 合法/非法合成样例；
- readiness、路径选择、blocking 保留和 V3 binding Schema 测试；
- 新文档：`REQ-20260905-02`、`BIZ-20260905-02`、`DEV-20260905-02`、`BUG-20260905-02`。

设计已决定未来完整 `RuleParseResultV3` 和 `FactBindingRequest 3.0.0`；当前因业务 blocking 未生成真实
RuleParseResultV3 或 handoff。SqlBot 当前只支持 2.0.0，未来必须在其仓库独立升级。

### 7. 收尾状态（会话恢复后已全部完成）

1. Ruff/format/Mypy 已对新旧全部文件复查通过；此前报告的长行随格式化一并修复。
2. `scripts.export_contract_schemas` 已重新运行，V3 binding Schema/样例与模型一致（SHA-256 幂等）。
3. 完整默认离线测试最终结果：`116 passed, 3 deselected`。
4. `git diff --check` 通过；`.obsidian/workspace.json` 无差异。
5. 本 PROG 与 `docs/进度文档.md` 已更新最终测试数字与状态。
6. Schema v5 migration 未实现、未执行，仅保留计划。
7. 未生成真实 handoff；16 个 blocking 全部保留，等待业务确认后生成新 catalog/candidate digest。

### 8. 下一会话建议命令

```powershell
cd D:\Python\pyWorkspace\RuleAgent
.\.venv\Scripts\python.exe -m scripts.export_contract_schemas
.\.venv\Scripts\ruff.exe check .
.\.venv\Scripts\ruff.exe format --check .
.\.venv\Scripts\mypy.exe
.\.venv\Scripts\pytest.exe
git diff --check
git status --short
```

如需重建离线报告：

```powershell
.\.venv\Scripts\python.exe -m scripts.analyze_v3_agent2_readiness `
  --reference-root 'D:\Python\pyWorkspace\RuleDataReferences' `
  --output-dir 'D:\Python\pyWorkspace\RuleDataReferences-recovery\agent2-readiness'
```

不要运行 MongoDB persistence、DeepSeek provider、SQL Server 或 SqlBot 命令。

### 9. Git 状态约束

RuleAgent 当前包含大量未提交 V3 恢复和本任务修改，全部属于现有工作；不得 reset、checkout、覆盖或
回退。没有提交或推送。私有 RuleDataReferences 固定仓库保持干净；恢复产物位于仓库外同级目录。

## 最终离线验证与收尾（会话恢复后）

会话恢复后仅执行离线验证、readiness 报告再生与文档收尾。本阶段没有访问或修改 MongoDB、没有调用
DeepSeek、没有连接 SQL Server、没有生成或执行 SQL、没有修改 SqlBot，也没有实现或执行 Schema v5
migration。

- `python -m scripts.export_contract_schemas` 成功；重复导出前后 V3 Schema 与样例 SHA-256 完全一致。
- `ruff check .` 通过；`ruff format --check .` 对本任务涉及的
  `scripts/export_contract_schemas.py` 与 `src/rule_reader/domain/rules/__init__.py`
  执行 import 合并格式化后通过，152 个文件全部符合格式。
- `mypy` 通过，55 个源码文件无问题。
- 完整默认离线测试 `python -m pytest -q`：**116 passed, 3 deselected**。
- `git diff --check` 通过（仅 Windows CRLF 提示，无空白错误）；`.obsidian/workspace.json` 无差异；
  未提交、未推送。
- 重新运行 `scripts.analyze_v3_agent2_readiness` 再生 4 份脱敏产物，输出固定为
  `selectedPath=B`、`ready=false`、`blockingCount=16`、计划 `ruleVersions=0`、`handoffs=0`、
  `requiresSqlBotContractUpgrade=true`；4 份文件 SHA-256 与再生前完全一致，可确定性重复。
- 脱敏复核：对 4 份产物执行私有规则 8 字符滑动窗口比对与模式扫描。所有中文片段均为生成器硬编码
  文案，两份 JSON 无中文；无私有规则正文、枚举值、物理字段、SQL、凭据或本机路径。唯一文本重合是
  私有规则条件表达式中英文 token `report_release_status` 的 8 字符子串与公开逻辑 factCode
  `report.release_status`、`task.offline_report_release_flag` 的既有词根重合，属已接受的 V3
  恢复交付物逻辑词汇（REQ 要求 blocking 报告保留逻辑事实），不构成正文泄漏。
- 本日最终状态：readiness=false；12 项门禁 fail、2 项 blocked、provenance 闭包与不可执行状态
  2 项 pass；16 项 blocking 全部保留等待业务逐项确认；`RuleParseResultV3=0`、`handoff=0`；
  SqlBot 需在其仓库独立升级 `FactBindingRequest 3.0.0` intake。
