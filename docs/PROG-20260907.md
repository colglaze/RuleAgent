# PROG-20260907：V3 confirmed 交付与 V3 恢复候选持久化到本机正式 MongoDB

- 状态：`COMPLETED_V3_DELIVERY_PERSISTED_SCHEMA_V5`
- 前序记录：[PROG-20260906](PROG-20260906.md)
- 依据需求/设计：[REQ-20260906-01](REQ-20260906-01-v3-mongodb-persistence.md)、
  [BIZ-20260906-02](BIZ-20260906-02-v3-mongodb-persistence.md)、
  [DEV-20260906-02](DEV-20260906-02-v3-mongodb-persistence.md)
- 数据来源：仓库内 [generated-rules/report-release-v3-confirmed/](../generated-rules/README.md)
  与本机私有 `RuleDataReferences` bundle（固定 commit、原 digest 校验通过）

## 用户授权

用户明确指令："把仓库里的 MongoDB 数据持久化到当前电脑的 MongoDB 中"。该指令构成本次
真实写入授权；授权范围限于把仓库已归档、已通过离线门禁的 V3 数据写入本机 `rule_reader`
正式库，不包括调用 DeepSeek、连接 SQL Server、修改 SqlBot 或任何 Git 写操作。

## 落库前只读检查（2026-09-07）

本机实例（`.env` 指向 `rule_reader` 库）为**原始正式库**，不是 2026-09-06 换机重建后的
空实例：

- Schema v3（migrations v1-v3，`app_metadata` service 0.8.0，最后更新 2026-08-24）；
- `rule_versions` 2 条：`@20260818T092511165521Z-562eabd40e5a`（V1）与
  `@20260824T080726492666Z-562eabd40e5a`（V2）；
- `fact_binding_handoffs` 33 条（Phase 1.10 幂等写入记录）；
- 无 v4/v5 集合，无 V3 记录。

因此本次实际持久化的是两块缺失的 V3 数据；历史 2 条规则与 33 条交接无需恢复、原样保留。

## 执行 1：V3 confirmed 交付持久化（Schema v5）

命令：`python -m scripts.persist_report_release_v3_delivery --artifact-dir
generated-rules/report-release-v3-confirmed`

- 产物信任锚校验通过（manifest SHA-256 = 冻结常量 `0ef3af69…`，五文件哈希逐一匹配），
  写前闭包门禁（blocking=0、readiness 16/16、请求身份/数量）全部通过后才初始化数据库；
- 结果 `persistedAndVerified`：数据库升级 v3→v5（仅新建
  `rule_structure_candidates_v3`、`rule_versions_v3`、`fact_binding_handoff_batches_v3`
  及唯一索引，历史集合与数据不变）；
- 落库规则版本 `REPORT_RELEASE_ALL_001@20260905T172407000000Z-f285643e5b2b-82dbd05a800a`，
  rule payload SHA-256 `fb6c1e3c…`，batch SHA-256 `0df35b35…`，18 条
  `FactBindingRequest 3.0.0`（`requestId=<ruleVersion>#<factCode>`）；
- `legacyCollectionCounts` 复核：`rule_versions=2`、`fact_binding_handoffs=33`、
  `rule_structure_candidates_v3=0`（写入前后一致）。

## 执行 2：V3 恢复候选持久化（rule_structure_candidates_v3）

命令：`python -m scripts.persist_report_release_v3_recovery --reference-root
../RuleDataReferences`

- 从私有 bundle 确定性重建恢复 payload（bundle digest 校验 fail-fast）；
- 结果 `persistedAndVerified`：candidateId
  `REPORT_RELEASE_ALL_001@f285643e5b2b-f2bf1c99d27c`，catalog digest
  `f2bf1c99d27c379559fff1e0311ffa8a202759ce466be6262a5611dc28c8518d` 与仓库归档
  `report-release-v3-recovery-20260905/` 记录一致；7 个确认事实、19 个规则节点
  （3 active / 16 blocked）、`executable=false`、`validatedBlockedCandidate`；
- payload SHA-256 `368e1389…`，回读比对通过，`inserted=true`。

## 幂等与独立复核

- 第二次执行交付持久化脚本 → `existingAndVerified`、`ruleInserted=false`、
  `batchInserted=false`，零重复插入；
- 全库只读终检：7 个集合计数为 `rule_versions=2`、`fact_binding_handoffs=33`、
  `rule_structure_candidates_v3=1`、`rule_versions_v3=1`、
  `fact_binding_handoff_batches_v3=1`、`schema_migrations=5`（v1-v5 齐全）、
  `app_metadata=1`（schema_version 5、service 0.12.0）；
- batch 复核：18 个 wrapper 的 `request_id`/`fact_code` 均唯一且互不相同，顶层
  `request_ids` 与存储顺序逐项一致，`contract_version` 全部为 `3.0.0`，每个 wrapper
  的 canonical payload SHA-256 经独立重算全部匹配；
- 恢复记录 `payload_sha256`、`status`、`executable=false` 与执行摘要一致。

## 边界与未做事项

- 未调用 DeepSeek/任何 Provider，未连接 SQL Server，未生成/执行 SQL，未修改 SqlBot；
- 未提交、未推送；本文件与进度文档更新为仅有的仓库文件改动；
- 未运行 integration 测试（`RULEREADER_TEST_MONGODB_URI` 与
  `RULEREADER_TEST_MONGODB_ALLOW_WRITE` 仍未配置）；本次落库不等价于 integration 验收；
- 仓库内 `mongodb-snapshot-20260906/` 快照描述的是换机重建后的另一实例，与本机原始正式
  实例无关，历史数据从未丢失。

## 下一任务

不变的两个决策点：

1. [BUG-20260906-05](BUG-20260906-05-agent1-v2-real-call-convergence-blocker.md) 关闭路径
   由用户选择（有界重试 / 两阶段解析改造 / 沿用 reviewed_import）；
2. 配置两个 loopback 环境变量后运行隔离 integration（当前正式库已是 Schema v5，
   integration 仍使用随机 `rule_reader_test_<uuid>` 隔离库，不受本次落库影响）。

## 文档事实纠偏（同日追加，仅改文档，不改应用逻辑）

授权边界：仅修改文档；不调用真实 Provider、不连接 MongoDB、不运行 integration、不提交、不推送。
本节不改动上文落库记录，只做事实对齐与证据分级。

1. **BUG-20260906-05 标题编号**：文件标题误写为"BUG-20260906-04"，已修正为"BUG-20260906-05"；
   文件名与既有引用原本正确，未改动。
2. **证据分级与缺口**：该缺陷的 5 份运行日志未留存到本机可审计位置
   （`artifacts/v2-agent1-real-runs-20260906/` 在当前工作区不存在），原始审计日志构成证据缺口；
   "模型在明确缺失清单反馈下连续多次输出相同候选"保留为分析推断（原文"连续 5 次"已更正为
   "连续多次"，实际为 run 4/5 各自第 2-6 次尝试），反馈传递、幂等缓存与适配器环节未被系统性
   排除；默认离线基线 `203 passed, 6 deselected` 已于本日复现（`.venv/Scripts/python.exe -m
   pytest -q`，7.65s）。
3. **两阶段表述更正**：当前真实运行可用的生成分支只有 ruleStructure 单阶段；confirmed V3
   成功来自 reviewed_import（本日已落库，见上文），与两阶段方案无关。"结构/案例两阶段"待设计、
   待真实 Provider 验证，不保证收敛或消除输出截断；[DEV-20260902-02](DEV-20260902-02-agent1-v3-prompt.md)
   已补"验证状态边界"，[BUG-20260906-05](BUG-20260906-05-agent1-v2-real-call-convergence-blocker.md)
   已补路径②立项要求（先冻结 REQ/DEV、总调用预算、阶段门禁、失败退出与离线验收；真实
   Provider 验证与数据库写入分别保留独立授权）。
4. **旧机器状态去当前化**：进度文档中 2026-09-06 的 Git 位置（`D:\LeStoreDownload\Git\cmd\git.exe`）、
   dubious ownership 与 `D:/Python/pyWorkspace/RuleAgent` safe.directory 指引、"工作区大量未提交/
   未跟踪"等，已标注为旧机器历史记录；"当前完整默认离线测试 128 passed"改为"当时"。2026-09-07
   实测：纠偏开始时本地 `main` = `origin/main` = 实时远端（`git ls-remote`）= `01ddae0`
   （`docs: record 20260906 delivery commit bad6fd3`），工作区 clean；本轮文档改动保持未提交。
5. **MongoDB 状态对齐**：2026-09-06 快照（业务集合 0 条、基础设施 5 条文档）限定为换机重建
   实例的当时状态，存于 `generated-rules/mongodb-snapshot-20260906/`；本机原始正式库历史数据
   （2 条规则、33 条交接）从未丢失，本日 V3 Schema v5 落库见上文。README 与技术设计文档中
   "最新文档证据为 2026-09-05 的 Schema v4 与一条 V3 recovery 记录，实时状态未重新确认"的
   过时陈述已更新；文档中的历史记录不构成数据库备份。
6. **数量核对**：实际代码为 7 个 HTTP 端点（`src/rule_reader/api/routes.py`：根路径、
   `/health/live`、`/health/ready`、`/api/v1/config`、`POST /api/v1/rules/parse`、版本回读、
   fact-binding-requests）、5 个 CLI 命令（`serve`/`check-config`/`init-db`/`parse`/
   `persist-handoffs`）、14 个 `scripts/*.py`；现有文档无聚合计数陈述
   （[PROG-20260818](PROG-20260818.md) 的"四个 HTTP 端点"为 2026-08-18 当时事实），按约定
   不新增统计文档。
7. **验证**：`.venv/Scripts/python.exe -m pytest -q` → **203 passed, 6 deselected**（离线默认
   套件，不依赖 Provider/MongoDB）；Markdown 相对链接检查 **556 个全部有效**；`git diff --check`
   通过。本节仅改文档：未调用 Provider、未连接 MongoDB、未运行 integration、未提交、未推送。
