# RuleReader 实施文档

- 状态：`RULE_SCHEMA_V3_PERSISTENCE_IMPLEMENTED_OFFLINE`
- 更新日期：2026-09-06
- 当前需求：[REQ-20260818-01](REQ-20260818-01-vibe-coding-bootstrap.md)
- 当前决策：[BIZ-20260818-01](BIZ-20260818-01-phase1-local-documents.md)
- 技术栈决策：[BIZ-20260818-02](BIZ-20260818-02-python-langgraph.md)
- 运行时与模型决策：[BIZ-20260818-03](BIZ-20260818-03-python311-deepseek.md)
- Phase 1.0 需求：[REQ-20260818-02](REQ-20260818-02-backend-skeleton.md)
- Phase 1.0 决策：[BIZ-20260818-04](BIZ-20260818-04-backend-fastapi-mongodb.md)
- Rule Parser 需求：[REQ-20260818-03](REQ-20260818-03-rule-parser.md)
- Rule Parser 技术方案：[DEV-20260818-02](DEV-20260818-02-rule-parser.md)
- Rule Version 需求：[REQ-20260818-04](REQ-20260818-04-rule-version-persistence.md)
- Rule Version 技术方案：[DEV-20260818-03](DEV-20260818-03-rule-version-persistence.md)
- Rule Contract v2 需求：[REQ-20260819-01](REQ-20260819-01-rule-contract-v2-agent2-handoff.md)
- Rule Contract v2 技术方案：[DEV-20260819-01](DEV-20260819-01-rule-contract-v2.md)
- Fact Binding Handoff 需求：[REQ-20260824-01](REQ-20260824-01-mongodb-fact-binding-handoff.md)
- Fact Binding Handoff 技术方案：[DEV-20260824-01](DEV-20260824-01-mongodb-fact-binding-handoff.md)
- V3 Agent 2 readiness 需求：[REQ-20260905-02](REQ-20260905-02-v3-agent2-handoff-readiness.md)
- V3 blocking 业务确认：[BIZ-20260906-01](BIZ-20260906-01-v3-blocker-business-confirmation.md)
- V3 Slice 3-6 技术方案：[DEV-20260906-01](DEV-20260906-01-rule-parse-result-v3.md)
- V3 Schema v5 持久化需求：[REQ-20260906-01](REQ-20260906-01-v3-mongodb-persistence.md)
- V3 Schema v5 业务决策：[BIZ-20260906-02](BIZ-20260906-02-v3-mongodb-persistence.md)
- V3 Schema v5 技术方案：[DEV-20260906-02](DEV-20260906-02-v3-mongodb-persistence.md)
- Schema 2.0 业务审核修订需求：[REQ-20260827-01](REQ-20260827-01-schema2-business-review-remediation.md)
- Schema 2.0 业务审核修订方案：[DEV-20260827-01](DEV-20260827-01-schema2-business-review-remediation.md)

## Phase 0：Vibe Coding 基线

### 交付内容

- 项目专属 `AGENTS.md`。
- PRD、技术设计、实施、进度四类权威入口文档。
- 第一阶段 REQ、范围 BIZ 和当日 PROG。
- 长期总体方案与当前实施范围的清晰区分。

### DoD

- 所有入口文档非空、相互链接且阶段边界一致。
- 第一阶段明确为本地 Markdown Rule Parsing Agent。
- Wiki 和其他长期模块均被标记为当前阶段范围外。
- 下一阶段的前置决策和任务已记录。

## Phase 1：本地文档 Rule Parsing Agent

Phase 1 按以下最小切片顺序推进。一个切片达到 DoD 后再进入下一个。

### 1.0 后端骨架

状态：`COMPLETE`（2026-08-18）

- 创建 Python `3.11.9`、FastAPI 和锁文件工程。
- 实现 Pydantic Settings、脱敏配置检查和 `.env.example`。
- 实现 PyMongo Async 连接、健康检查和基础设施 migration。
- 提供 CLI、HTTP 端点、单元测试、integration 测试和 README。

DoD：服务可启动、配置可读、MongoDB 可从空库初始化且重复执行安全；详细标准见 [REQ-20260818-02](REQ-20260818-02-backend-skeleton.md)。

完成证据见 [PROG-20260818](PROG-20260818.md)。

### 1.1 契约与样例

状态：`COMPLETE`（2026-08-18）

- 创建对应 `DEV` 文档。
- 冻结 Markdown 规则模板、JSON Schema、语义规则、错误码和来源元数据。
- 冻结 Python `3.11.9` 工具链、DeepSeek 模型与调用策略，以及 LangGraph state、node、edge、路由和失败状态。
- 建立正常、反向、边界、嵌套、例外、缺失和歧义样例。

DoD：不调用模型即可对固定 JSON 候选执行完整 Schema/语义校验，样例与预期结果可重复验证。

### 1.2 工程骨架与领域校验

状态：`COMPLETE`（2026-08-18）

- 根据 DEV 创建锁定 Python `3.11.9` 的最小工程和测试结构。
- 实现领域类型、Schema 校验和语义校验。
- 不接入真实模型或外部系统。

DoD：默认测试离线通过，非法候选不会形成有效草稿。

### 1.3 本地文档适配器

状态：`COMPLETE`（2026-08-18）

- 实现受控目录读取、编码处理、路径安全和内容哈希。
- 将文档内容与来源元数据交给应用用例。

DoD：正常与文件错误场景均有自动化测试，无法读取的文档不会调用模型。

### 1.4 模型解析适配器

状态：`COMPLETE`（2026-08-18）

- 接入 DEV 确认的 DeepSeek 模型和结构化输出策略。
- 使用 LangGraph 串联文档读取、模型解析、Schema 校验、语义校验和错误路由。
- 实现超时、限流、空响应、非法 JSON 和有限重试。
- 使用测试替身覆盖默认测试，真实 Provider 测试显式启用。

DoD：模型候选必须经过两级校验；所有 Provider 失败路径以及 LangGraph node、edge 和终止 state 可重复验证。

### 1.5 可执行闭环

状态：`COMPLETE`（2026-08-18）

- 通过 DEV 确认的 CLI 或 API 入口串联单文件解析。
- 输出待审核草稿、来源信息或结构化错误。
- 补齐回归测试和使用文档。

DoD：代表性本地 Markdown 可以端到端转换为可校验草稿；失败不留下有效产物；进度文档有完整证据。

Phase 1.1-1.5 完成证据见 [PROG-20260818](PROG-20260818.md)。规则结果保持 `draft`、`executable=false`，进入后续阶段前需先由业务人员审核 JSON 契约和字段映射候选。

### 1.6 不可变草稿版本归档

状态：`COMPLETE`（2026-08-18）

- 增加 MongoDB Schema v2 和 `rule_versions` 集合约束。
- 实现应用仓储端口和 MongoDB 适配器。
- 为 HTTP/CLI 增加显式持久化入口，并提供按精确版本号回读。
- 保存并回读一版《项目报告释放规则》。

DoD：迁移可从空库和 v1 幂等执行；默认解析不入库，显式保存不可变且可回读；真实数据库中存在一条通过校验的项目报告规则草稿版本；详细标准见 [REQ-20260818-04](REQ-20260818-04-rule-version-persistence.md)。

完成证据见 [PROG-20260818](PROG-20260818.md)。正式 `rule_reader` 数据库已升级到 Schema v2，并保存一条《项目报告释放规则》不可变 draft 版本。

### 1.7 规则契约 2.0 与事实交接

状态：`COMPLETE`（2026-08-19）

- 新解析改用 Schema `2.0.0`，以封闭表达式 AST 保存公式、派生事实和日期计算；
- 实现事实闭包、派生环、表达式/操作符类型、映射和测试案例的确定性校验；
- 实现三值解释器，案例为 `indeterminate` 或与期望不一致时拒绝候选；
- 仓储同时回读 Schema `1.0.0` 与 `2.0.0`，不覆盖或自动迁移历史版本；
- 增加按版本导出 `FactBindingRequest[]` 的 HTTP 入口，旧 Schema 返回稳定 409；
- 通过离线回归覆盖 `R+0.1>=F` 和产品 759 互斥分档错误。

DoD：新解析只生成 Schema `2.0.0` 待审核草稿；复杂公式不可丢失为单事实阈值；事实交接只包含非派生事实；默认测试、MongoDB integration、Ruff、Mypy 和依赖检查通过。详细标准见 [REQ-20260819-01](REQ-20260819-01-rule-contract-v2-agent2-handoff.md)。

完成证据见 [PROG-20260819](PROG-20260819.md)。真实来源重新解析与人工审核仍是 [BUG-20260819-01](BUG-20260819-01-rule-ast-semantic-loss.md) 的剩余验收，不由离线契约实现替代。

### 1.8 可验证的事实绑定请求契约

状态：`COMPLETE`（2026-08-24）

- 冻结旧 `FactBindingRequest 1.0.0`，新增破坏性的 `2.0.0` 主版本和显式 HTTP 版本选择；
- 新版完整声明逻辑实体、字段、筛选、聚合、时间范围、结果、来源证据和结构化不确定性；
- 从现有规则草稿无法确定的实体键、物理字段、筛选完整性、聚合函数和时间语义以 `unresolved + blocking` 导出，不猜测 SQL；
- 提供可复现的 Draft 2020-12 Schema、合法未决/非法漏字段/旧版样例；
- 以 Pydantic 与 dev-only `jsonschema` 双重验证真实导出和样例，并覆盖默认旧版、显式新版与未知版本 HTTP 行为。

DoD：静态 Schema 与领域模型一致；所有新版请求包含六类 `queryRequirements`；正反样例与旧版兼容性测试通过；默认测试不访问模型、网络或数据库；RuleReader 没有新增 Agent 2、Text-to-SQL 或数据库读取能力。完成证据见 [PROG-20260824](PROG-20260824.md)。

### 1.9 仅输出 2.0 与真实规则重解析

状态：`COMPLETED`（2026-08-24）

- 事实绑定 HTTP 默认和显式调用只允许 `FactBindingRequest 2.0.0`，旧 `1.0.0` 仅保留离线历史兼容测试；
- Parser Prompt 升级并明确完整 AST、互斥分支、事实契约、测试覆盖、不确定性及无 SQL/凭据要求；
- 使用缺陷指定的同一来源执行真实 DeepSeek 重解析，先完成确定性和业务结构审计，再保存新的不可变 Schema `2.0.0` 草稿；
- 回读保存结果并验证每个非派生事实均可导出严格的 `FactBindingRequest 2.0.0`，阻断不确定性只阻止后续 SQL，不阻止草稿保存。

DoD：运行时不再输出 `1.0.0`；新版本来源哈希正确，公式/分支/案例通过审计和解释器；MongoDB 精确回读一致；事实请求通过 Pydantic 与独立 JSON Schema；未实现 Agent 2、Text-to-SQL 或数据库访问。

两批真实 `deepseek-v4-flash` 调用均被可信校验拒绝后，用户明确授权不再调用 Provider 的受控 `reviewed_import`。应用升级到 `0.7.0`，以独立来源标识和 `reviewed-import-v1` 构建同一哈希来源的草稿；41 个事实、65 个条件和 20 个确定性案例通过专项审计。新版本已不可变保存并精确回读，33 个非派生事实全部导出严格的 `FactBindingRequest 2.0.0`。每个请求均含阻断不确定性，因此只能审核，不能开始 SQL。该阶段 DoD 已满足，详见 [PROG-20260824](PROG-20260824.md)。

### 1.10 MongoDB 不可变事实交接

状态：`COMPLETED`（2026-08-24）

- 应用升级到 `0.8.0`，MongoDB Schema 从 v2 幂等升级到 v3；
- 新增 RuleReader 唯一写入、SqlBot 只读的 `fact_binding_handoffs`，以 `_id/request_id` 和 `rule_version + fact_code` 保护唯一身份；
- 交接用例只从 `rule_versions` 精确回读 `RuleParseResultV2`，逐条执行 Pydantic、仓库内 Draft 2020-12 Schema、安全文本和 canonical SHA-256 门禁；
- 相同 request ID 与哈希重复写入幂等且保留首次时间，不同 payload 哈希 fail-fast 且禁止覆盖；
- 新增显式 `rule-reader persist-handoffs --rule-version <version>`，该路径不创建模型客户端、不读取源 Markdown、不修改历史规则；
- 指定真实 V2 规则已交接 33 条，重复执行插入 0 条；33 条均保留 blocking uncertainties，历史 V1 交接为 0 条。

DoD：新集合、所有权、不可变语义与索引通过 migration/integration 验证；正式 33 条 payload 均通过 Pydantic 与静态 JSON Schema 并匹配 canonical hash；目标 V2 和历史 V1 的原始 BSON 哈希在交接前后不变；Ruff、严格 Mypy、默认测试、MongoDB integration 与依赖检查通过。详细标准见 [REQ-20260824-01](REQ-20260824-01-mongodb-fact-binding-handoff.md)，完成证据见 [PROG-20260824](PROG-20260824.md)。

### Post-Phase 1.10：Schema 2.0 业务审核阻断修订

状态：`COMPLETED_VALIDATED_NOT_PERSISTED`（2026-08-27）

- 应用升级到 `0.9.0`，MongoDB Schema 保持 v3；
- 保留旧 profile 和不可变版本，新增 source-hash-bound `reviewed-import-v2` 修订 profile；
- 修正事实粒度/参数、原始数据复合条件、案例输入值域门禁和 6 个不可靠映射；
- 以 31 个案例覆盖全部 67 个条件的 `pass/fail` 两侧；
- 只在本地导出新的 V2 草稿和 34 条 blocking 事实请求，不调用 DeepSeek 或 MongoDB。

DoD：新版本保持 `draft`、不可执行且未持久化；规则和事实请求通过全部确定性、Pydantic、静态 Schema 和安全门禁；旧规则/请求导出哈希不变；默认测试、Ruff、严格 Mypy 和依赖检查通过。详细标准见 [REQ-20260827-01](REQ-20260827-01-schema2-business-review-remediation.md)，完成证据见 [PROG-20260827](PROG-20260827.md)。

### Post-Phase 1.10：DeepSeek 重试、审计与幂等性治理

状态：`COMPLETED`（2026-08-27）

- 应用升级到 `0.10.0`，MongoDB Schema 保持 v3，规则 Schema 保持 `2.0.0`；
- 可重试 Provider/候选失败在下一次调用前执行确定性、有上限的指数退避，总尝试数继续为 `max_retries + 1`；
- Provider 适配器提取并校验 completion ID 和可用 token usage，LangGraph 在成功与最终错误中输出逐次及汇总审计；
- HTTP `Idempotency-Key` 和 CLI `--idempotency-key` 进入同一进程内协调器，同键同身份合并在途调用并重放成功结果，同键异身份 fail closed；
- 原始幂等键、来源正文、Prompt、Provider 原始响应和凭据不进入审计/日志；完成缓存有界且不保存失败。

DoD：退避边界、尝试次数、逐次 outcome、Provider ID/token 汇总、顺序/并发重放、冲突、失败后重试和 LRU 淘汰均有确定性测试；reviewed import 的旧 JSON 形状不变；默认测试不调用 DeepSeek、网络或 MongoDB；Ruff、严格 Mypy 和依赖检查通过。详细标准见 [REQ-20260827-02](REQ-20260827-02-deepseek-retry-audit-idempotency.md)，完成证据见 [PROG-20260827](PROG-20260827.md)。

### Post-Phase 1.10：Rule Schema 3.0 Slice 1/2

状态：`COMPLETED_OFFLINE_BLOCKED_CANDIDATE`（2026-09-03 收尾）

- 完成 `RuleStructureCandidateV3`、确认事实目录、静态 JSON Schema、结构/闭包/类型/优先级校验和
  多 outcome 解释器；
- 完成第 1.3 节项目报告/原始数据独立提取与规则块 hash；
- 以脱敏合成夹具覆盖合法/非法目录和候选；
- 只读核对固定私有 bundle、XLSX 与 7 个视图，离线生成 `REPORT_RELEASE` 候选；
- 历史候选摘要记录 14 个 blocked 节点，明确记录代码映射、公式、时间与事实契约缺口；
  不生成测试案例、RuleParseResultV3、模板请求或持久化记录。

DoD：静态 Schema 与 Pydantic 一致；默认测试保持离线；真实私有资料只读校验通过；V1/V2 默认路径与
历史对象不变；缺失确认事实和来源冲突显式阻断。首轮过早结论已修订，
历史记录见 [PROG-20260903](PROG-20260903.md)，恢复后的验证见
[PROG-20260905](PROG-20260905.md)。

2026-09-05 换机恢复以用户新确认的 XLSX 有效性和有序规则来源重新生成真实 profile。当前结果为
7 个确认事实、19 个节点（3 active、16 blocked）；与历史 14 blocked 的差异显式保留，不用旧摘要
覆盖重新读取的来源证据。

### Post-Phase 1.10：REPORT_RELEASE V3 显式有界纠错

状态：`ENGINEERING_COMPLETE_BUSINESS_CONFIRMATION_REQUIRED`（2026-09-03）

- 现有 V3 最小 LangGraph 增加最多 3 次的共享请求预算、内存 previousCandidate/retryFeedback、
  业务缺口优先停止、分支保留和逐次审计；固定一次的兼容入口与命令默认值保持不变。
- 真实任务在第 1 次请求后发现模型声明的事实缺口并停止，剩余 2 次未使用；候选同时因 ruleCode
  全局重复被 Schema 拒绝，candidate=null，不进入测试案例阶段。
- 完整默认离线测试 204 passed、3 deselected；Ruff、格式检查、严格 Mypy 和 pip check 通过。
- 没有修改 Schema 或确认事实，没有连接 MongoDB/SqlBot/SQL，没有保存、发布或执行候选。

DoD：请求硬上限、错误反馈、业务缺口停止、上下文不变、候选不修补、脱敏错误和审计均有
MockTransport/纯领域回归；历史提前停止及待核对项见 [PROG-20260903](PROG-20260903.md) 和
[BUG-20260903-02](BUG-20260903-02-agent1-v3-single-call-schema-rejection.md)。

### Post-Phase 1.10：V3 MongoDB 换机恢复

状态：`COMPLETED`（2026-09-05）

- 应用升级到 `0.11.0`，MongoDB Schema v4 新增 `rule_structure_candidates_v3`；
- source-bound catalog 和 candidate 作为一个不可变 payload 保存，candidate ID 与 canonical hash
  确定性生成；
- 同 ID 同内容幂等回读，不同内容冲突且不覆盖；
- V3 数据不进入 V1/V2 `rule_versions` 或 V2 `fact_binding_handoffs`；
- 保存状态固定为 `validatedBlockedCandidate / executable=false`。

DoD：默认离线测试、migration integration、当前 MongoDB 首次写入/重复写入/精确回读和历史集合不变
检查通过。详细标准见 [REQ-20260905-01](REQ-20260905-01-v3-mongodb-recovery.md)。

### Post-Phase 1.10：V3 Agent 2 readiness 初始门禁

状态：`COMPLETED_HISTORICAL_BLOCKED_STAGE`（2026-09-05）

- 对 V2/V3 语义等价性、16 项 candidate blocking 和 16 条 Agent 2 readiness 门禁执行确定性分析；
- 业务确认前严格输出 0 个规则版本与 0 个 handoff，仅生成确认清单和 Schema v5 计划；
- 冻结 `FactBindingRequest 3.0.0` 契约，禁止把 V3 静默降级为 V2 交接。

DoD：阻断报告、静态 Schema、Pydantic 校验和 blocked 回归均离线通过；历史设计见
[DEV-20260905-02](DEV-20260905-02-v3-agent2-handoff-readiness.md)。

### Post-Phase 1.10：V3 业务确认

状态：`COMPLETED`（2026-09-06）

- 用户裁决 12 项缺失事实、3 项来源值冲突和 1 项确认枚举下不可达分支；
- confirmed catalog 扩展为 19 个事实，candidate 19 个节点全部 active、0 blocking；
- 物理 mapping 与 binding profile 不在 RuleReader 中补造，继续留待下游元数据审批。

DoD：业务裁决、证据边界和值域取舍已记录于
[BIZ-20260906-01](BIZ-20260906-01-v3-blocker-business-confirmation.md)。

### Post-Phase 1.10：V3 Slice 3-6 离线结果、导出与 readiness

状态：`COMPLETED_OFFLINE_READY_TRUE`（2026-09-06）

- 生成一个不可变、待审核、`executable=false` 的 `RuleParseResultV3`；
- 生成 18 条 `FactBindingRequest 3.0.0`，mapping 保持 unresolved；
- 20 个测试案例覆盖全部 outcome、资格首个命中、postGate、exclusion 与不确定结果；
- readiness 16/16 全部通过，`ready=true`；固定输入与 `generatedAt` 可确定性再生。

DoD：领域/Schema/导出/readiness 测试、完整默认离线测试、Ruff、格式检查、严格 Mypy 和
依赖检查通过。未访问 MongoDB、DeepSeek 或 SQL Server，未生成 SQL，未修改 SqlBot；Schema v5
持久化与 SqlBot 3.0.0 intake 仍需独立授权。详细实现见
[DEV-20260906-01](DEV-20260906-01-rule-parse-result-v3.md)。

### Post-Phase 1.10：V3 交付 MongoDB Schema v5 持久化

状态：`COMPLETED_OFFLINE_AWAITING_REAL_WRITE_AUTHORIZATION`（2026-09-06；同日按
[BUG-20260906-03](BUG-20260906-03-v3-persistence-authorization-and-integrity-gaps.md) 验收修订）

- 应用版本 `0.12.0`；MongoDB 迁移授权分级：普通服务、`rule-reader init-db`、V1/V2 持久化、
  V2 handoff 与 V3 恢复默认只到 Schema v4，仅 V3 持久化脚本在全部离线产物校验通过后显式请求
  v5（`rule_versions_v3` 与 `fact_binding_handoff_batches_v3`，各含冻结的唯一索引）；数据库
  已是 v5 时普通应用正常启动，不降级、不重写；
- 脚本以冻结的 manifest 自身 SHA-256 为信任锚绑定唯一获批交付
  （ruleVersion `REPORT_RELEASE_ALL_001@20260905T172407000000Z-f285643e5b2b-82dbd05a800a`、
  18 条请求、20 个测试案例、ready=true、blocking=0、executable=false），无绕过参数；
- 应用层 `v3_persistence` 服务/端口与 MongoDB 适配器：insert-only、同 hash 幂等保留首次时间、
  异 hash 冲突；写前一次性完成 blocking、readiness 16/16、hash 闭包、请求身份/数量门禁；写入后
  精确回读（拒绝乱序 wrapper、重复 request ID/factCode、wrapper/batch 时间不一致、naive 时间）
  并复核历史集合数量不变；CLI 错误统一脱敏 JSON（code/message/retryable/details）并以非零码
  退出；摘要区分 `persistedAndVerified`/`recoveredAndVerified`/`existingAndVerified`；
- confirmed profile 的 parserVersion 冻结为 `0.11.0`，应用版本升级不改变既有产物身份
  （[BUG-20260906-02](BUG-20260906-02-confirmed-profile-version-coupling.md)）。

DoD：Schema v5 迁移分级（默认 v4/显式 v5/非法 target/v5 库普通启动）、回读加固（乱序/重复/
时间/契约一致）、唯一获批产物绑定、CLI 脱敏、恢复摘要均有先失败后转绿的测试；默认测试、
Ruff、格式检查、严格 Mypy 和依赖检查通过。未向真实 MongoDB 写入数据、未运行持久化脚本、
未运行 integration 测试（未授权）、未调用 DeepSeek、未连接 SQL Server、未修改 SqlBot；真实
写入需用户单独授权。详细实现见
[DEV-20260906-02](DEV-20260906-02-v3-mongodb-persistence.md)。

## 后续阶段

Wiki、RuleReader 内的 Agent 2/SQL/元数据能力、事实注册、规则执行、审批发布、草稿更新删除、诊断和销售查询仍属于长期方案。SqlBot 已作为独立 Agent 2 仓库接收事实绑定契约，但 DeepSeek SQL 生成仍需其独立 REQ/DEV。上述后续能力不能混入 RuleReader Phase 1。
